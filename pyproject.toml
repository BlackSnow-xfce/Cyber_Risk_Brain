[project]
name = "security-ai-platform"
version = "0.1.0"
description = "AI-driven security correlation and threat hunting platform"
requires-python = ">=3.11"

dependencies = [
    "requests",
    "python-dotenv"
]

[project.optional-dependencies]
test = [
    "pytest"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
