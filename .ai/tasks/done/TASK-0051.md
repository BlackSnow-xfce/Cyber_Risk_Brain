# TASK-0051 – Finding Explanation API Boundary

## Metadata

Task-ID: TASK-0051  
Phase: LIVE AI EXPLANATION API  
Status: DONE  
Owner: Codex  
Reviewer: Architect

## Freigabestatus

DONE – PASS / APPROVED durch Architect.

## Voraussetzungen

* TASK-0050: DONE / PASS / APPROVED
* Live-Findings-Pfad: Greenbone GMP XML → `UniversalFinding` → `FindingsQueryService`
* Validierte Capability: `FindingExplanationService` mit Input Contract `1.0`

## Ziel

Die bestehende Finding-Explanation-Capability über eine minimale FastAPI-Boundary für ein anhand seiner kanonischen Finding-ID ausgewähltes Live-Finding verfügbar machen.

Bevorzugter Endpoint:

`POST /api/findings/{finding_id}/explanation`

## Scope

* bestehende konfigurierte Finding-Quelle verwenden
* Finding-ID eindeutig selektieren
* Asset Context, Risk Readiness, Input Builder und Explanation Service wiederverwenden
* `FindingExplanationResult` API-kompatibel projizieren
* bestehende kontrollierte Failure-Status unverändert transportieren
* Finding-/Source-/Configuration-Fehler kontrolliert auf HTTP abbilden
* fokussierte API- und Application-Tests ohne Live-Provider-Aufruf
* kontrollierter Live-Acceptance-Test mit einem anderen DVWA-Lab-Finding nach grünen Tests

## Nicht-Ziele

Keine Änderung an Prompt, Input Contract, Result Contract, Reasoning-Regeln, RiskEngine, Decision, Evidence, Scoring, RAG, Agents, Ingestion, Frontend, Persistenz, Provider oder Modell-ID. Kein TASK-0052.

## Acceptance

* vorhandenes Finding erzeugt genau eine bestehende Explanation-Ausführung
* unbekannte Finding-ID liefert definierten HTTP-Fehler
* fehlende oder ungültige Finding-Quelle liefert kontrollierten HTTP-Fehler
* bestehende Explanation-Fehlerstatus werden ohne Neuinterpretation transportiert
* RiskEngine-Aufrufe bleiben `0`
* Live-Acceptance durchläuft Finding → API Boundary → validierten Result Contract

## AIDP-Abschluss

Nach Implementierung TASK-0051 nach REVIEW verschieben, TO-ARCHITECT aktualisieren und TO-CODEX auf WAITING setzen. Keine Selbstfreigabe und kein TASK-0052.

## Implementierungsergebnis

### API Boundary

Implementierter Endpoint:

`POST /api/findings/{finding_id}/explanation`

Die FastAPI-Schicht delegiert an `FindingExplanationUseCase`. Der Use Case verwendet ausschließlich bestehende Capabilities:

* `FindingsQueryService`
* `AssetContextQueryService`
* `RiskReadinessService`
* `FindingExplanationInputBuilder`
* `FindingExplanationService`
* `OpenAIFindingExplanationModel`

Der bestehende Input Contract `1.0`, Result Contract, Prompt, Provider, Modell und sämtliche TASK-0050-Guardrails blieben unverändert.

### HTTP-Fehlersemantik

* unbekannte Finding-ID: HTTP `404`, sichere generische Meldung
* fehlende Finding- oder Asset-Context-Konfiguration: HTTP `503`
* ungültige, nicht lesbare oder mehrdeutige konfigurierte Quelle: HTTP `500`, sichere generische Meldung
* bestehende kontrollierte Explanation-Status (`CONFIGURATION_ERROR`, `PROVIDER_ERROR`, `TIMEOUT`, `INVALID_OUTPUT`): HTTP `200` als unveränderter `FindingExplanationResult`, ohne neue fachliche Interpretation

### Automatisierte Validierung

* fokussierte neue und relevante bestehende Tests: `39 passed`
* vollständiger Python-Testlauf: `82 passed`
* FastAPI OpenAPI-Auflösung für den neuen POST-Endpoint: erfolgreich
* Frontend TypeScript: Exit Code `0`
* `git diff --check`: Exit Code `0`
* automatisierte Live-Provider-Aufrufe: `0`
* RiskEngine-Aufrufe im fokussierten Use-Case-Test: `0`

## Live Acceptance

### Finding und Auditmetadaten

* Finding: `Missing 'HttpOnly' Cookie Attribute (HTTP)`
* Finding-ID: `d9ec4339-83c9-4cd0-a81b-bc461669a295`
* Source: `greenbone`
* Vendor Severity: `Medium`
* Observed Asset: `172.18.0.18`
* Canonical Asset: `asset-lab-dvwa-001`
* Asset Criticality: `LOW`
* Asset Context Source: `product-owner:dvwa-lab-classification`
* Provider-Aufrufe: `1`
* Provider: OpenAI API (`openai`)
* Modell: `gpt-5.6-terra`
* Result Status: `GENERATED`
* Input Contract Version: `1.0`
* Input Digest: `28ae1336f277df2285a528bc6e6b9e23cbdcb44796294876e3bdeda1f4292176`
* Source References: `product-owner:dvwa-lab-classification`
* RiskEngine-Aufrufe: `0`

Verwendete Fact IDs:

* `asset.canonical_id`
* `asset.criticality`
* `asset.observed_identifier`
* `finding.source`
* `finding.title`
* `finding.vendor_severity`
* `risk.criticality_state`
* `risk.detection_state`
* `risk.exposure_state`
* `risk.mitre_state`
* `risk.readiness_status`
* `risk.threat_intelligence_state`

### Tatsächlich erzeugte und lokal validierte AI-Explanation

Summary – `CONTEXTUAL_INFERENCE`:

> The finding concerns a missing HttpOnly attribute on HTTP cookies and is rated Medium by the reporting source. The affected asset is classified as LOW criticality, but the overall assessment has insufficient context because exposure, detection, threat-intelligence, and MITRE context were not evaluated.

Fact References: `finding.title`, `finding.vendor_severity`, `asset.criticality`, `risk.readiness_status`, `risk.exposure_state`, `risk.detection_state`, `risk.threat_intelligence_state`, `risk.mitre_state`

Technical Reasoning – `GENERAL_SECURITY_REASONING`:

> The HttpOnly cookie attribute directs supporting browsers not to expose a cookie to client-side script. If a relevant cookie lacks this attribute, successful script execution in the browser could make that cookie more accessible to script, which can increase the consequences of client-side scripting flaws. The attribute is a defense-in-depth control and its absence alone does not establish that script execution, cookie theft, session compromise, or exploitation has occurred.

Fact References: keine, vertragsgemäß.

Technical Reasoning – `CONTEXTUAL_INFERENCE`:

> This finding is specifically reported as "Missing 'HttpOnly' Cookie Attribute (HTTP)" by Greenbone, with a vendor severity of Medium.

Fact References: `finding.title`, `finding.source`, `finding.vendor_severity`

Organizational Relevance – `CONTEXTUAL_INFERENCE`:

> The canonical asset asset-lab-dvwa-001, observed as 172.18.0.18, has an authoritative LOW criticality classification. This provides context for prioritization, but does not determine technical exploitability or impact.

Fact References: `asset.canonical_id`, `asset.observed_identifier`, `asset.criticality`, `risk.criticality_state`

Organizational Relevance – `CONTEXTUAL_INFERENCE`:

> No conclusion is available about whether the affected service is exposed, whether detection is available, whether threat intelligence matches this issue, or which MITRE tactic applies, because each of those inputs is NOT_EVALUATED.

Fact References: `risk.exposure_state`, `risk.detection_state`, `risk.threat_intelligence_state`, `risk.mitre_state`, `risk.readiness_status`

Uncertainty Statement – `CONTEXTUAL_INFERENCE`:

> The assessment is marked INSUFFICIENT_CONTEXT. The supplied facts do not establish the cookie's purpose or sensitivity, whether it is a session or authentication cookie, the presence of related client-side scripting weaknesses, exploitability, active exploitation, or the asset's exposure.

Fact References: `risk.readiness_status`, `risk.exposure_state`, `risk.detection_state`, `risk.threat_intelligence_state`, `risk.mitre_state`

### Missing Context

* `exposure`: `NOT_EVALUATED`
* `detection_available`: `NOT_EVALUATED`
* `threat_intelligence_match`: `NOT_EVALUATED`
* `mitre_tactic`: `NOT_EVALUATED`

## Scope-Nachweis

* Keine RiskEngine-Ausführung im Live Acceptance.
* Keine Frontend-Datei durch TASK-0051 geändert.
* Keine Änderung an TASK-0050-Verträgen, Prompt, Provider oder Modell.
* Keine Secrets oder Provider-Rohantworten dokumentiert.
* Kein TASK-0052 erstellt.

## Architecture-Review-Entscheidung

* Review Result: `PASS / APPROVED`
* Freigabe: Architect
* Die minimale API-Boundary exponiert die bestehende TASK-0050-Capability ohne Parallelarchitektur oder neue AI-Reasoning-Logik.
* `FindingExplanationResult` Contract `1.0` und sämtliche Fact-/Inference-/General-Security-Reasoning-Guardrails blieben unverändert.
* HTTP-Fehlersemantik, vollständige Regression, fokussierte Tests und Live Acceptance sind akzeptiert.
* Live Result Status: `GENERATED`
* Provider-Aufrufe: `1`
* RiskEngine-Aufrufe: `0`
* Die bestehende IP-Identifier-Beschränkung ist nicht blockierend und bleibt separater Scope.

TASK-0051: `DONE / PASS / APPROVED`. TO-CODEX bleibt `WAITING`. Kein TASK-0052 wurde erstellt.
