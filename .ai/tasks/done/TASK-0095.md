# TASK-0095 – Reproducible MVP Demo Baseline

Status: DONE
Review: PASS / APPROVED
Reviewer: Architect

## Scope

This task defines and verifies the local runtime baseline for the existing
Finding→Explanation→Incident Command Center MVP path. It introduces no product
logic, new domain data, provider call, or IAM infrastructure.

## Verified baseline

The canonical slice is documented in `MVP-DEMO-BASELINE.md`. Existing runtime
data verifies the DistCC Finding, Metasploitable2 asset, CVE-2004-2687 and
`incident-task0077-distcc-live` with relationship `rel-finding`.

The ignored local `.env` explicitly configures the existing report, asset
context, incident persistence and the exact Finding ID through
`AI_FINDING_EXPLANATION_ALLOWED_IDS`. `OPENAI_API_KEY` is never copied or
printed. `VITE_API_BASE_URL` remains a frontend-process setting.

## Pre-flight and validation

The baseline files and identifiers were checked read-only. Backend import and
FastAPI app construction succeeded without a provider call. The configured
pre-flight verified 69 findings, the canonical Finding, asset and incident
files, the persisted `investigation_candidate` relationship, the exact
authorization ID and provider-key presence without exposing its value.

The existing focused retrieval/explanation suite passed (22 tests), the
broader focused suite passed (28 tests), the full Python regression passed
(446 tests), TypeScript `npx tsc --noEmit` completed with exit code 0, and
`git diff --check` passed.

## Architect review

Architect review: PASS / APPROVED. MVP Pre-Flight: PASS. No product logic,
tests, provider paths, or new feature changed. The complete baseline,
validation and review history is preserved.

No TASK-0096 was created.

Updated: 2026-08-19
