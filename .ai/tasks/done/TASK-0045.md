# TASK-0045 – First Real Greenbone Dataset Import

## Metadata

Task-ID: TASK-0045
Phase: FIRST LIVE INTELLIGENCE
Status: DONE
Owner: Codex
Reviewer: Architect

## Typ

Backend Validation / Real Dataset Import

## Priorität

Critical

## Ziel

Zum ersten Mal einen tatsächlich von Greenbone/OpenVAS erzeugten GMP-XML-Export aus DVWA oder einem vergleichbaren kontrollierten verwundbaren Testsystem über die in TASK-0044 implementierte Ingestion Boundary verarbeiten und die daraus erzeugten realen `UniversalFinding`-Objekte technisch validieren.

Dieser Task validiert ausschließlich die reale Dateikompatibilität und das bestehende Mapping. Er erweitert weder den Datenfluss hinter `UniversalFinding` noch die Architecture Baseline 1.0.

## Verbindliche Grundlagen

* `AGENTS.md`
* `ARCHITECTURE.md` – Architecture Baseline 1.0 (`BASELINE`)
* ADR-0001 bis ADR-0007 (`ACCEPTED`)
* TASK-0044 – First Live Intelligence Ingestion Contract (`DONE`, `PASS / APPROVED`)
* `ingestion.greenbone_xml`
* `core.models.UniversalFinding`

## Eingangsartefakt

Erforderlich ist eine tatsächlich von Greenbone/OpenVAS erzeugte GMP-XML-Exportdatei aus:

* DVWA, oder
* einem vergleichbaren, ausdrücklich kontrollierten verwundbaren Testsystem.

Die Datei muss als echter Scanner-Export nachvollziehbar bezeichnet sein. Eine dokumentationsbasierte Schema-Fixture, ein manuell konstruiertes XML oder simulierte Scanner-Ausgabe erfüllt diese Voraussetzung nicht.

## Verbindliche Ausführungsregel

Vor jeder Produktänderung read-only im Repository und im bereitgestellten Arbeitsverzeichnis nach einem echten Greenbone/OpenVAS-Export suchen.

Falls kein solcher Export vorhanden ist:

* keinen künstlichen Export erzeugen,
* keine vorhandene Schema-Fixture als real deklarieren,
* keine Produktdatei ändern,
* TASK-0045 im Status `READY` belassen,
* die Ausführung kontrolliert stoppen,
* exakt dokumentieren, dass eine echte Greenbone/OpenVAS-GMP-XML-Datei aus einem kontrollierten Ziel benötigt wird, einschließlich des erwarteten bereitgestellten Dateipfads.

Das Fehlen des realen Exports autorisiert keine Scanner-Ausführung, Infrastrukturinstallation oder Scope-Erweiterung.

## Scope

### 1. Herkunft und Eignung des realen Exports prüfen

* Herkunft als Greenbone/OpenVAS-Export aus einem kontrollierten Testziel dokumentieren.
* Datei und Größe dokumentieren.
* Sicherstellen, dass keine Secrets, Zugangsdaten oder unzulässigen personenbezogenen beziehungsweise produktiven Daten eingecheckt werden.
* Prüfen, ob Ablage im Repository gemäß Repository-Policy und Dateigröße sinnvoll ist.
* Falls die Datei nicht eingecheckt werden darf, Herkunft, verwendeten lokalen Pfad und reproduzierbare Identifikationsmerkmale dokumentieren, ohne sensible Inhalte zu übernehmen.

### 2. Reale Datei über bestehende Boundary importieren

* Ausschließlich `ingestion.greenbone_xml.load_greenbone_xml_findings()` beziehungsweise den bestehenden Parserpfad verwenden.
* Prüfen, ob der reale Export ohne Änderung akzeptiert wird.
* Anzahl der Greenbone-Resultate im realen Export und Anzahl erzeugter `UniversalFinding`-Objekte dokumentieren.
* Mindestens ein reales Resultat muss erfolgreich und deterministisch zu `UniversalFinding` transformiert werden.

### 3. Repräsentative Feldabbildung validieren

Für repräsentative reale Findings mindestens prüfen:

* `source`,
* `id` und Rückführbarkeit auf die Greenbone-Result-UUID,
* `title` beziehungsweise NVT-Name,
* `vendor_severity`,
* `asset` beziehungsweise Host-Referenz, soweit vom realen Export geliefert,
* CVE beziehungsweise CVE-Bezug, soweit im realen Resultat vorhanden und ohne Änderung des bestehenden `UniversalFinding` abbildbar,
* weitere bereits vorhandene kanonische Felder ausschließlich soweit sie vom Scanner tatsächlich geliefert werden.

Fehlende Informationen dürfen weder erzeugt noch plausibilisiert werden. Scanner-spezifische XML-Strukturen dürfen die Ingestion Boundary nicht verlassen.

### 4. Minimal notwendige Parserkorrektur

Falls der reale Export von der dokumentationsbasierten Struktur aus TASK-0044 abweicht:

* ausschließlich `ingestion.greenbone_xml` minimal an die tatsächlich beobachtete Greenbone-XML-Variante anpassen,
* keine zweite Boundary und kein paralleles Mapping erstellen,
* die reale Abweichung und die konkrete Korrektur dokumentieren,
* fokussierte Tests nur für die tatsächlich aufgetretene Formatvariante ergänzen oder anpassen,
* bestehende Contract-Tests weiterhin bestehen lassen.

Wenn die Abweichung eine Änderung des kanonischen Domain-Modells oder der Architecture Baseline verlangen würde, nicht autonom lösen, sondern als objektiven Blocker an den Architect zurückgeben.

### 5. Reales Sample-Artefakt

* Den echten Export nur dann als klar gekennzeichnetes Test-/Sample-Artefakt aufnehmen, wenn Repository-Policy, Sensitivität und Dateigröße dies erlauben.
* Das Artefakt als Scanner-Export aus einem kontrollierten Testziel kennzeichnen; niemals als produktive Intelligence ausgeben.
* Falls keine Aufnahme erfolgt, die Validierung nachvollziehbar dokumentieren, ohne den Export zu kopieren oder synthetisch nachzubauen.

## Zulässige Änderungen

Nur wenn ein echter Export vorhanden ist und eine Änderung objektiv erforderlich wird:

* minimale Anpassung an `ingestion/greenbone_xml.py`,
* minimale Anpassung an `ingestion/__init__.py`, nur falls für den bestehenden Pfad erforderlich,
* fokussierte Tests unter `tests/ingestion/`,
* echter, kontrollierter Greenbone-Testexport unter einer klar gekennzeichneten Test-/Sample-Ablage, sofern zulässig,
* `.ai/tasks/ready/TASK-0045.md` nach `.ai/tasks/review/TASK-0045.md` verschieben und Ergebnis dokumentieren,
* `.ai/handoff/TO-ARCHITECT.md` aktualisieren,
* `.ai/handoff/TO-CODEX.md` auf `WAITING` setzen.

Keine Änderung ist erforderlich, wenn der bestehende Parser den realen Export bereits korrekt verarbeitet; Validierung und AIDP-Dokumentation bleiben dann die Deliverables.

## Explizit nicht Bestandteil

* Graph-Integration
* RiskEngine-Anbindung
* Correlation-Verarbeitung
* DecisionEngine-Anbindung
* Explainability-Verarbeitung
* UI-Anzeige oder UI-Änderungen
* Scanner-API oder automatische Scan-Ausführung
* Scanner-Orchestrierung
* Scheduler, Queues oder Messaging
* Persistenz- oder Datenbankänderungen
* neue Architektur oder Änderungen an Architecture Baseline 1.0
* ADR-0008 oder andere neue ADRs
* allgemeines Scanner-Framework
* Umbau oder Erweiterung von `UniversalFinding`
* neue kanonische Modelle
* erfundene oder simulierte Scanner-Ausgaben als Ersatz für den realen Export

## Akzeptanzkriterien

* Ein tatsächlich von Greenbone/OpenVAS erzeugter GMP-XML-Export aus einem kontrollierten Ziel wurde verarbeitet.
* Der reale Export wird von der bestehenden Ingestion Boundary akzeptiert oder die Boundary wurde ausschließlich minimal an die beobachtete reale Formatvariante angepasst.
* Mindestens ein echtes Scanner-Resultat wurde deterministisch in `UniversalFinding` überführt.
* Source Identity/Provenance ist über Greenbone-Quelle und unveränderte Result-UUID erhalten.
* Anzahl importierter Scanner-Resultate und Anzahl erzeugter `UniversalFinding`-Objekte sind dokumentiert.
* Repräsentative reale Findings wurden auf die freigegebenen Feldabbildungen geprüft.
* Keine scanner-spezifische XML-Struktur verlässt die Boundary.
* Keine fehlenden Scanner- oder Businessinformationen wurden erzeugt oder plausibilisiert.
* Architecture Baseline 1.0 und ADR-0001 bis ADR-0007 bleiben unverändert.
* Keine Graph-, Risk-, Correlation-, Decision-, Explainability- oder UI-Schicht wurde vorgezogen.
* TASK-0045 befindet sich nach erfolgreicher Ausführung in `REVIEW`.

## Pflichtprüfungen

Nur nach Verarbeitung eines echten Exports:

* realen Import mit nachvollziehbarem Befehl ausführen und Ergebnis dokumentieren,
* relevante fokussierte Ingestion-Tests ausführen,
* `python -m pytest` ausführen,
* taskbezogenes `git diff --check` ausführen,
* keinen TypeScript-Typecheck, Build oder Browser-Test ausführen, sofern keine Frontend-Datei geändert wurde.

## Abschlussbedingungen

Bei erfüllten Akzeptanzkriterien:

* TASK-0045 von `.ai/tasks/ready/` nach `.ai/tasks/review/` verschieben.
* Reale Exportherkunft, Importbefehl, Resultatanzahl, Finding-Anzahl und repräsentative Feldprüfung vollständig dokumentieren.
* `.ai/handoff/TO-ARCHITECT.md` vollständig aktualisieren.
* `.ai/handoff/TO-CODEX.md` auf `WAITING` setzen.
* Keinen Folgetask oder ADR eigenständig erstellen.

Ohne echten Export bleibt TASK-0045 unverändert `READY` und die Ausführung endet mit einer präzisen Anforderung an das fehlende Eingangsartefakt.

## Ergebnis des Implementation Agent

Verwendeter realer Export und Herkunft:

* Echter Greenbone/OpenVAS-GMP-XML-Export eines lokalen DVWA-Testziels.
* Lokaler Pfad: `C:\Users\sinii\Desktop\report-f70ee8ed-8e00-417b-90f2-0e451f307fc3.xml`.
* Dateigröße: 211145 Bytes. Die Originaldatei wurde ausschließlich gelesen und nicht in das Repository kopiert.
* XML war ohne Dokumenttyp/Entity-Deklaration lesbar. Report-ID `f70ee8ed-8e00-417b-90f2-0e451f307fc3`, Task `DVWA-Lab-Scan`, Scan-Status `Done` und GMP-Version `22.7` wurden direkt aus der Datei verifiziert.

Importierte Greenbone-Resultate:

* Der Scanner-Gesamtbestand ist im inneren `result_count/full` mit 66 deklariert.
* Der aktive Exportfilter weist `result_count/filtered = 5` aus; der `<results>`-Container enthält tatsächlich 5 direkte `<result>`-Elemente.
* Die 66 Scanner-Gesamtergebnisse sind ausdrücklich nicht mit 66 exportierten XML-Resultaten gleichzusetzen.

Erzeugte UniversalFindings:

* Die bestehende Datei-Boundary `ingestion.greenbone_xml.load_greenbone_xml_findings()` erzeugte 5 `UniversalFinding`-Objekte aus 5 exportierten Scanner-Resultaten.
* Ein wiederholter Import derselben realen Datei lieferte identische Ergebnisse.

Repräsentative Feldvalidierung:

* Alle fünf Greenbone-Result-UUIDs wurden unverändert als `UniversalFinding.id` übernommen; `source` ist jeweils `greenbone`.
* NVT-Name wurde jeweils als `title`, Threat als `vendor_severity` und Host `172.18.0.18` als `asset` übernommen.
* Die bekannten fünf Titel sowie Threat/Severity-Kombinationen (`Critical`/`10.0`, `Medium`/`5.0`, `Medium`/`4.8`, `Low`/`2.6`, `Low`/`2.1`) wurden direkt gegen die reale XML geprüft.
* Hostname (`dvwa` beziehungsweise `dvwa.greenbone-community-edition_default`), Port (`general/tcp`, `80/tcp`, `general/icmp`), NVT-OID, QoD (70 oder 80), vorhandene Description sowie Creation-/Modification-Time wurden in der realen XML validiert. Diese Felder sind nicht Bestandteil des bestehenden `UniversalFinding`-Vertrags und wurden daher nicht in das kanonische Modell aufgenommen oder anderweitig plausibilisiert.

Geänderte Dateien:

* `ingestion/greenbone_xml.py` – Result-Selektion minimal auf direkte `<result>`-Kinder eines `<results>`-Containers begrenzt.
* `tests/ingestion/fixtures/greenbone-gmp-schema-sample.xml` – bestehende, weiterhin ausdrücklich synthetische Schema-Fixture um die real beobachtete verschachtelte `detection/result`-Struktur ergänzt.
* `.ai/tasks/ready/TASK-0045.md` nach `.ai/tasks/review/TASK-0045.md` verschoben und mit den Live-Ergebnissen dokumentiert.
* `.ai/handoff/TO-ARCHITECT.md` für das Architecture Review aktualisiert.
* `.ai/handoff/TO-CODEX.md` gemäß AIDP auf `WAITING` gesetzt.

Durchgeführte Prüfungen:

* Live-Import über `load_greenbone_xml_findings()`: 5 XML-Resultate, 5 kanonische Findings, deterministische Wiederholung, Exit Code 0.
* `python -m pytest -p no:cacheprovider tests\ingestion\test_greenbone_xml_contract.py`: 3 Tests erfolgreich, Exit Code 0.
* `python -m pytest -p no:cacheprovider`: 22 Tests erfolgreich, Exit Code 0.
* Taskbezogenes und repositoryweites `git diff --check`: Exit Code 0.
* `cd frontend; npx tsc --noEmit`: Exit Code 0. Kein Frontend-Build oder Browser-Test, da keine Frontend-Datei geändert wurde.

Risiken oder Blocker:

* Reale Strukturabweichung: Das erste Scanner-Resultat enthält unter `detection/result` einen weiteren gleichnamigen Metadatentag. Die bisherige globale Suche interpretierte diesen fälschlich als sechstes Finding und scheiterte am dort absichtlich fehlenden Host.
* Die minimale Korrektur selektiert ausschließlich Resultate direkt unter `<results>`; keine zweite Importstrecke, DVWA-Sonderlogik oder Erweiterung des kanonischen Vertrags wurde eingeführt.
* Port, OID, numerische Severity, Threat, QoD, Description und Zeiten sind in der XML vorhanden, können aber mit dem unveränderten kanonischen Vertrag nur teilweise abgebildet werden. Dies ist für TASK-0045 kein Blocker und wurde nicht außerhalb des freigegebenen Scopes gelöst.

## Architektur-Review

Status: APPROVED
Bewertung: PASS
Bemerkungen:

* Scope und Diff wurden unabhängig geprüft. Es wurden ausschließlich die bestehende Greenbone-Ingestion-Boundary, die fokussierte Schema-Fixture und die vorgeschriebenen AIDP-Artefakte geändert.
* Der reale Desktop-Export wurde erneut direkt verarbeitet. Report-ID, Task, Status, GMP-Version, 66 Scanner-Gesamtresultate, 5 gefilterte direkte XML-Resultate und 5 deterministisch erzeugte `UniversalFinding`-Objekte wurden bestätigt.
* Die Selektion direkter `<result>`-Kinder von `<results>` ist die kleinste fachlich korrekte Korrektur. Das reale verschachtelte `<result>/<detection>/<result>` wird nicht länger als Finding fehlinterpretiert.
* Die Fixture enthält keine sensiblen Werte des realen Exports. Sie reproduziert ausschließlich die beobachtete Verschachtelungsform; der bestehende Test mit exakt einem Finding deckt die Regression ab.
* `UniversalFinding` blieb unverändert. UUID, Quelle, NVT-Name/Title, Threat/Vendor Severity und Host/Asset werden vertragsgemäß übernommen. Hostname, Port, NVT-OID, numerische Severity, QoD, Description und Zeitinformationen bleiben scannerseitige Boundary-Daten.
* Keine Risk-, Correlation-, Threat-Intel-, Decision-, Explainability-, Graph-, Persistenz-, API-, Frontend- oder DVWA-spezifische Integration wurde eingeführt. Architecture Baseline 1.0 und ADR-0001 bis ADR-0007 bleiben unverändert.
* Review-Prüfungen: fokussierte Ingestion-Tests 3 erfolgreich, vollständiger Python-Testlauf 22 erfolgreich, `git diff --check` Exit Code 0.

Freigabe: Architect, 2026-08-16

## Nächster Task

WAITING – kein weiterer READY-Task ist durch dieses Review autorisiert.
