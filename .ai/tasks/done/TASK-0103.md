# TASK-0103 – Target-Side Runtime Observation Adapter

Status: DONE
Review: PASS / APPROVED
Reviewer: Architect

## Administrative closure

The controlled target-side runtime adapter is approved. It translates the
minimal TCP/3632 signal into `SecurityObservationInput` and delegates trusted
metadata assignment to the TASK-0102 Creation Boundary, producing
`SecurityObservation 1.0` with `TARGET_SIDE` independence.

Source-/test-acceptance remains PASS. The previously blocked live runtime
acceptance was subsequently completed in the Docker lab and is now PASS.

## Real Docker-Lab Runtime Acceptance

- Target: `metasploitable2` / `172.18.0.19`
- Service: DistCC / `TCP/3632`
- Test client: `predator-testclient` / `172.18.0.20`
- Network: `greenbone-community-edition_default`
- Runtime check: `nc -vz 172.18.0.19 3632` succeeded
- Target-side collection: `tcpdump` on `metasploitable2:eth0`
- Capture: 10 packets captured, 10 received by filter, 0 dropped by kernel

The real observation was represented through the existing path:

```text
TargetSideRuntimeSignal
→ TargetSideRuntimeObservationAdapter
→ SecurityObservationInput
→ TASK-0102 Creation Boundary
→ SecurityObservation 1.0
```

Observed contract output:

- `observation_id`: `observation:distcc:runtime:2026-08-20`
- `observation_type`: `network_traffic`
- `observed_at`: `2026-08-20T20:40:06.065695+00:00` (process representation time;
  the supplied capture summary did not include a packet timestamp)
- `target_resource`: `asset-lab-metasploitable2-001`
- `source_identity`: `sensor:target-side-tcpdump`
- `collection_method`: `packet_capture`
- `independence`: `target_side`
- `provenance.source_reference`:
  `tcpdump:metasploitable2:eth0:172.18.0.20->172.18.0.19:3632`
- `provenance.raw_observation_reference`:
  `docker:greenbone-community-edition_default:metasploitable2:tcpdump:10-packets`
- `contract_version`: `1.0`

The result proves only that TCP communication to DistCC was observed
target-side. It does not prove CVE exploitation, RCE, compromise, verification,
confirmation or Incident Truth. No Evidence, Correlation, Verification or LLM
operation occurred.

No Correlation, Evidence, Incident, sensor, persistence, API, UI or LLM
integration was added. MVP 1.0 remains historically
`DONE / PASS / APPROVED / FROZEN`.

TASK-0104 was not created.

## Validation

- Architect decision: PASS / APPROVED
- Focused tests: 31 passed
- Full Python regression: 484 passed
- TypeScript `npx tsc --noEmit`: Exit Code 0
- `git diff --check`: passed

Updated: 2026-08-20
