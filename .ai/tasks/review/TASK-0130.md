---
task_id: TASK-0130
phase: POST-MVP RISK MANAGEMENT
allowed_scope: frontend/src/workspaces/soc/findings/FindingsPresentationDensity.ts, frontend/src/workspaces/soc/findings/FindingsWorkspace.tsx, frontend/src/workspaces/soc/findings/FindingsWorkspace.test.tsx, frontend/src/workspaces/soc/findings/FindingsToolbar.tsx, frontend/src/workspaces/soc/findings/FindingsList.tsx, frontend/src/workspaces/soc/findings/FindingListItem.tsx, frontend/src/workspaces/soc/findings/FindingDetailsPanel.tsx, frontend/src/workspaces/soc/findings/FindingThreatIntelligenceSection.tsx, frontend/src/workspaces/soc/findings/FindingThreatIntelligenceSection.test.tsx, frontend/src/workspaces/soc/findings/FindingExplanationSection.tsx, frontend/src/workspaces/soc/findings/FindingRiskContextSection.tsx, frontend/src/workspaces/soc/findings/FindingRiskContextSection.test.tsx
prohibited_actions: core/enterprise_context/asset_context.py, core/enterprise_context/asset_business_context.py, application/asset_context.py, application/finding_asset_context.py, application/asset_business_context.py, application/finding_asset_business_context.py, application/business_impact_readiness.py, application/risk_readiness.py, application/finding_risk_priority.py, core/decision/business_context.py, core/decision/business_impact.py, core/decision/business_impact_builder.py, core/decision/decision_engine.py, core/decision/risk_engine.py, core/decision/priority_engine.py, core/decision/recommendation_engine.py, core/predator_engine.py, analysis/risk_engine.py, ingestion/**, infrastructure/**, runtime/**, .env, frontend/src/mocks/**, frontend/src/workspaces/risk-manager/**, frontend/src/workspaces/executive/**, frontend/src/platform/**, frontend/src/components/topbar/**, backend/**, provider/**, .ai/references/**, .ai/tasks/done/TASK-0127.md, .ai/tasks/done/TASK-0128.md, .ai/tasks/done/TASK-0129.md, .ai/tasks/**/TASK-0131.md
validation_requirements: python tests, frontend tests, typescript, production build, git diff --check
product_owner_gate: true
---
# TASK-0130 - Authoritative Service Impact Profile & Business-Impact Classification Readiness

Status: REVIEW / REWORK REQUIRED
Reviewer: Architect
Architect Task-Specification Review: PASS / APPROVED
Architect Implementation Review: FAIL / REWORK REQUIRED
Product Owner Live Acceptance: PENDING / NOT READY

Execution-ID: c84345dd-1f83-4ba7-b775-329636f6a92a
Execution Commit: 7c1852d2
Execution Status: SUCCESS

Architect Review Finding: The nested `ThreatIntelligenceResult` rendered inside
`FindingThreatIntelligenceSection` retains its global `h5`, `h6`, default body,
caption, and overline typography. The Findings wrapper applies the local density
only to nested Chips, so the right-column TI facts, references, and headings do
not satisfy the REWORK-10 density contract. Focused TI density coverage asserts
only the constants and does not render/assert the nested result typography.

## REWORK 10 - Local Findings Analyst Typography Density

Product Owner live acceptance accepts the traffic-light and structured authority
presentation, but requires a denser typography scale across both Findings columns.
This rework is presentation-only and must not alter workspace geometry, data,
authority, parser, domain, risk, readiness, provenance, or business behavior.

### Ownership and Local Architecture

- `FindingsToolbar`, `FindingsList`, and `FindingListItem` own the search/list/card
  presentation in the left/center column.
- `FindingDetailsPanel` owns the right-column title, base metadata, and Linked
  Incidents. `FindingThreatIntelligenceSection`, `FindingRiskContextSection`, and
  `FindingExplanationSection` own their respective detail subsections.
- `FindingsWorkspace` owns composition only; geometry and column proportions stay
  unchanged. A local `FindingsPresentationDensity` module is the single reusable
  authority for typography/chip presentation constants, avoiding global-theme
  changes and divergent inline magic numbers.

### Exact Typography Contract

- Workspace/panel heading: `subtitle1`, `0.9375rem` (15px), line-height `1.35`,
  weight `700`.
- Section heading: `subtitle2`, `0.875rem` (14px), line-height `1.35`, weight `700`.
- Subsection heading: `subtitle2`, `0.8125rem` (13px), line-height `1.3`, weight
  `700`.
- Structural field label: `caption`, `0.6875rem` (11px), line-height `1.2`, weight
  `700`; preserve the approved casing, letter spacing, and traffic-light token.
- Primary field/list/detail value: `body2`, `0.75rem` (12px), line-height `1.35`.
- Help/explanatory text: `body2`, `0.75rem` (12px), line-height `1.4`.
- Reference/provenance/technical secondary text: `caption`, `0.6875rem` (11px),
  line-height `1.3`, `text.secondary`, with exact wrapping preserved.
- Search/input text: `0.8125rem` (13px), line-height `1.35`; local toolbar button
  text uses `0.75rem` (12px), line-height `1.25`.
- Small Chips remain outlined where already contracted, use height `20px`, and
  label text `0.6875rem` (11px), line-height `1.2`, with compact horizontal padding.
  Status color/text semantics and ordinary-value neutrality remain unchanged.
- 11px is the desktop minimum and is limited to labels/references/Chip labels;
  primary analyst data remains at least 12px.

### Column and Spacing Contract

The left column applies the scale to list heading, card title, severity Chip,
asset/source fields, and local search controls. The right column applies it to
Finding Details, base metadata, TI, Risk Context, Business Context/readiness,
Service Impact Profile, Technical Effects, Correlation/Evidence, Inputs, Missing
Requirements, Priority, AI Explanation, and Linked Incidents.

Do not change the accepted spacing identities: `AuthorityField` `0.125`, sibling
units `1.25`, repeated groups `1.5`, repeated Technical Effects parent `1.5`, and
individual effect record `1.25`. Component stacks may only have typography-owned
default margins removed where necessary; no structural Stack spacing or workspace
geometry may change. Preserve exact values, wrapping, grouping, section ordering,
traffic-light labels, status Chips, CVSS acronym display, and requirement
de-duplication.

### Focused Tests and Validators

Use existing rendering conventions to prove the shared local contract is applied
to both columns; heading/section/subsection/label/value/reference hierarchy;
proportional Chips and search controls; unchanged traffic-light semantics;
neutral severity/criticality/effect values; exact long-reference wrapping;
unchanged REWORK-6 spacing/grouping/order; no global-theme mutation; no duplicate
or fabricated content; and `business_impact = null`, `decision = null`, and
`recommendations = []`. Do not use screenshot, pixel, or computed-style tests.

Run focused workspace, TI, and Risk Context rendering tests; full frontend and
Python regressions; TypeScript; production build; `git diff --check`; and an exact
twelve-path Scope Guard. Do not create TASK-0131.

## REWORK 9 - Known-Fact Authority Guidance

Product Owner live acceptance found that the established facts in `What
PredatorAI knows` remain visually neutral. Correct only their local label-level
success presentation. No value, transported fact, authority, or behavior changes.

### Exact Known-Fact Rule

- Every item in authoritative `context.source_facts` is an established Finding
  fact supplied by the backend. Its structural fact label and the associated
  provenance/source label use restrained MUI `success.main` guidance. This covers
  `finding_id`, `source`, `title`, `vendor_severity`, and `observed_asset` without
  inventing or duplicating rows.
- When `context.asset_context.status === "resolved"`, the `Canonical asset`,
  `Asset criticality`, and associated `Source` structural labels use the same
  restrained success guidance. Do not apply it when canonical resolution is not
  established.
- Apply the success token through an actual local MUI theme-resolved styling path
  so the label is visibly differentiated in the live dark theme; a test-only data
  attribute is not sufficient. Preserve the approved compact uppercase,
  font-weight, line-height, and letter-spacing treatment. Do not modify the global
  theme or introduce hard-coded colors.

### Label Versus Value and Source Contract

Green means only that the fact is authoritatively known. Fact values remain
neutral `text.primary`; vendor severity and AssetCriticality magnitude never
become traffic-light states. Source names/references remain neutral or secondary
as already approved, while their structural `Source` labels use success guidance
when associated with an established fact. Never color `Critical` red or `LOW`
green based on magnitude.

### Focused Tests and Frozen Boundaries

Render a complete known-fact projection and explicitly prove success semantics on
Finding ID, Source, Title, Vendor Severity, Observed Asset, Canonical Asset, Asset
Criticality, and resolved-context Source labels. Prove their values remain neutral,
including `Critical` and `LOW`; source references remain secondary; unavailable
units remain warning; the mixed Correlation/Evidence parent remains neutral;
errors remain error; CVSS acronym/de-duplication remains correct; and all REWORK-6
spacing, grouping, wrapping, section order, Chips, accessibility, null/empty
boundaries, and REWORK-7/8 semantics remain unchanged. Avoid screenshots, pixel or
computed-color assertions. Run focused rendering tests, full frontend regression,
full Python regression, TypeScript, production build, `git diff --check`, and the
exact two-path Scope Guard. Do not create TASK-0131.

## REWORK 8 - Exact CVSS Label and Mixed-Parent Neutrality

Correct only the two remaining REWORK-7 presentation defects. No transported
identifier, parser, domain, authority, readiness, provenance, risk, or business
behavior may change.

### Mixed-Parent Semantic Contract

- A grouped parent may use restrained success guidance only when every relevant
  child authority state is positively established and compatible.
- A grouped parent may use warning guidance only when every relevant child state
  is unambiguously incomplete/unavailable and warning is semantically accurate.
- A parent with mixed positive and incomplete child states must remain neutral.
  The existence of one positive child must never establish parent success, and
  one warning child must never make a mixed parent warning.
- A genuine grouped failure may use error treatment only when existing surfaced
  failure semantics authorize it.
- Specifically, Correlation `NO_DATA`/incomplete plus available Evidence renders
  the `Correlation / Evidence` parent neutral, Correlation warning, and Evidence
  success. Child tone must not leak across sibling authorities.

### CVSS Acronym Contract

The human-readable missing-requirement label for
`supported_cvss_v3_effect:CVE-2004-2687` must render exactly as
`Supported CVSS v3 effect — CVE-2004-2687`. Preserve the raw identifier byte-for-
byte in transported data, mapping, and stable keys. Continue suppressing the
redundant raw identifier from normal analyst presentation. Do not introduce a
general normalization that mutates other identifiers or authoritative values.

### Focused Tests and Frozen Boundaries

Add explicit coverage for a mixed Correlation/Evidence group proving a neutral
parent, warning Correlation child, success Evidence child, no style leakage, and
unchanged underlying values. Correct the acronym test to require uppercase
`CVSS`, reject lowercase `cvss`, and prove the raw identifier remains unchanged
and usable for mapping/key identity. Preserve all REWORK-6 spacing (`0.125`,
`1.25`, repeated `1.5`, Technical Effect parent `1.5`, effect record `1.25`),
REWORK-7 traffic-light semantics, status Chips, neutral technical data, grouping,
wrapping, order, accessibility, de-duplication, and null/empty functional
boundaries. Run focused rendering tests, full frontend and Python regressions,
TypeScript, production build, `git diff --check`, and exact two-path Scope Guard.
Do not create TASK-0131.

## REWORK 7 - Traffic-Light Authority-State Presentation

Product Owner live acceptance rejected cyan/`info.main` as the primary visual
language. Replace it locally with a restrained traffic-light hierarchy that
classifies authority state without changing any transported value or semantic.

### Exact Semantic Presentation Contract

- Use existing MUI `success` semantics for an authority unit only when its state
  is unambiguously `AVAILABLE`, `READY`, or `RESOLVED`, or when the field is an
  authoritative fact known to be present in the enclosing resolved/available
  snapshot. The unit title and structural labels that identify that established
  authority may inherit restrained success coloring; ordinary values remain
  neutral and status values retain small outlined success Chips.
- Use existing MUI `warning` semantics for an authority unit whose explicit state
  is `UNAVAILABLE`, `NOT_FOUND`, `NOT_EVALUATED`, `UNKNOWN`, or incomplete. Its
  unit title and state-bearing structural labels may inherit restrained warning
  coloring; status values retain small outlined warning Chips. Missing facts and
  requirement names remain neutral data.
- Use existing MUI `error` semantics only for an explicitly surfaced integrity,
  contradiction, invalid-authority, parser/source, or other genuine failure.
  Preserve existing error Alerts where applicable. Never derive error coloring
  from vendor severity, asset/service criticality, BusinessImportance, or
  TechnicalEffectLevel.
- For mixed collections, determine presentation state per child authority record.
  In particular, each Threat Intelligence authority inherits its own explicit
  status; the containing section must not flatten mixed states into one color.
- Remove `info.main` as the default structural-label color. A label/title inherits
  success, warning, or error only when its associated unit state is unambiguous.
  State-ambiguous containers and neutral technical labels remain `text.primary` or
  `text.secondary`. URLs, provenance references, CVEs, vectors, timestamps,
  severities, criticalities, BusinessImportance, and HIGH/LOW/NONE effect values
  remain neutral. Color supplements, and never replaces, explicit status text.

### Missing Requirement Presentation

Render each requirement as the existing human-readable label only. Suppress the
second redundant raw machine identifier when it conveys the same requirement.
The underlying identifier remains untouched in the model and transport and still
drives stable keys and label mapping. Examples include `Business service`,
`Environment`, `Service criticality`, `Business context provenance`, `Service
impact profile`, and `Supported CVSS v3 effect — <CVE>`.

### Preserved Layout and Boundaries

Preserve all REWORK-6 layout contracts exactly: `AuthorityField` spacing `0.125`,
sibling units `1.25`, repeated groups `1.5`, repeated Technical Effect parent
`1.5`, and individual Technical Effect records `1.25`. Preserve wrapping,
grouping, section order, accessible text, status Chips, and error Alerts. No
global theme, shell, workspace geometry, API, parser, domain, authority, risk,
readiness, provenance, legacy, AI, or TASK-0127/0128/0129 behavior may change.
`business_impact` remains null, `decision` remains null, and `recommendations`
remains empty. Do not create TASK-0131.

### Focused Tests and Validators

Focused rendering tests must prove established units use success semantics;
unavailable/not-evaluated units use warning semantics; genuine surfaced failures
retain error semantics; mixed-state TI records are colored independently;
severity, criticality, effect levels, identifiers, references and missing
requirement names remain neutral; outlined status Chips and explicit status text
remain correct; human-readable requirement labels render without redundant raw
identifiers; authoritative data is unchanged; and REWORK-6 spacing, wrapping,
grouping, section order, null boundaries, and accessibility remain intact.
Avoid screenshots, pixel/computed-color assertions, or brittle geometry tests.
Run focused rendering tests, full frontend regression, full Python regression,
TypeScript, production build, `git diff --check`, and the exact two-path Scope
Guard.

## REWORK 6 - Repeated Technical Effect Rhythm

REWORK 5 placed the repeated-record spacing marker on each Technical Effect
record, where it controls internal fields, while the parent container still
separates repeated records with ordinary unit spacing. Correct only this spacing
ownership and add the missing multi-effect structural coverage.

### Exact Spacing Contract

- Preserve compact `0.125` spacing inside every `AuthorityField` label/value/
  reference association.
- Introduce or use a dedicated container whose children are exclusively the
  repeated Technical Effect records. That container owns `spacing={1.5}` and the
  stable repeated-record layout marker.
- Each individual Technical Effect record owns `spacing={1.25}` and the stable
  sibling-unit layout marker for its CVE, version, vector, C/I/A, source and
  observed fields. It must not declare repeated-record spacing internally.
- Status and reason remain outside the repeated-record container with their
  already-approved sibling-unit rhythm. No other REWORK-5 spacing is changed.

### Focused Multi-Effect Test

Render at least two distinct authority-complete Technical Effects with different
CVE identifiers, vectors, CIA values, sources/references and observed timestamps.
Assert that both effect groups render independently; each group contains only its
own exact values; the effects parent declares repeated-record spacing; each effect
record declares sibling-unit spacing; no cross-effect mixing occurs; status Chip
semantics remain unchanged; HIGH/LOW/NONE remain neutral; references remain exact
and wrapping-safe; and Business Impact, Decision and Recommendations remain null/
empty. Do not use screenshots, pixels or computed-spacing assertions.

Preserve every already-approved REWORK-3/4/5 presentation and all TASK-0127/
0128/0129/TASK-0130 authority and frozen global UI, theme, backend, API, parser,
legacy and AI boundaries. Run focused rendering tests, full frontend regression,
full Python regression, TypeScript, production build, `git diff --check`, and the
exact two-path Scope Guard. Product Owner acceptance remains pending/not ready.
Do not create TASK-0131.

## REWORK 5 - Live Dark-Theme Semantic Scanability

REWORK 4 applied correct semantic palette tokens but relied on small caption/body
text color alone. In the live dark theme this produces insufficient figure/ground
and weight contrast, while nested Stack spacing makes authority-unit boundaries
less obvious than the label-to-value association. REWORK 5 strengthens only the
local presentation cues and rhythm; it changes no transported value or semantic.

### Local Visual Treatment

- Structural labels use `info.main` plus a restrained overline-like treatment:
  existing MUI `Typography`, uppercase display through CSS `textTransform`,
  `fontWeight: 700`, compact line height, and modest theme-consistent letter
  spacing. Underlying label strings and accessible text remain unchanged. Labels
  must not become headings or status indicators.
- Explicit positive and incomplete status values use a small MUI `Chip`, with
  `size="small"`, `variant="outlined"`, and existing `success` or `warning`
  palette semantics. The exact status text remains the Chip label. Genuine
  surfaced failures retain existing MUI error `Alert` semantics. No ordinary
  value, technical-effect level, severity, source, or reference becomes a Chip.
- Ordinary values remain `text.primary`; references/provenance remain
  `text.secondary`. No hard-coded colors and no global-theme changes are allowed.

### Rhythm Contract

- Within one `AuthorityField`, label to value uses a compact 1-2px-equivalent MUI
  spacing; an optional reference/detail remains closely associated immediately
  below the value.
- Between sibling authority fields, use a clearly larger 10px-equivalent minimum
  MUI Stack spacing. Between repeated authority records/subgroups, use a 12px-
  equivalent or larger spacing already consistent with the section hierarchy.
- Preserve all REWORK-3 label/value blocks, grouped Inputs, wrapping-safe exact
  references, section separation/order, and responsive behavior. Do not use
  margins, breaks, transforms, clipping, or punctuation as layout.

### Tests and Validation

Focused tests must assert semantic presentation intent without screenshots or raw
pixel/color values: structural labels retain exact text and declare the local
label role/treatment; positive and incomplete states render explicit small
success/warning Chips; ordinary values/effect levels and missing-requirement names
do not render as status Chips; error text remains in an error Alert; field-internal
spacing is smaller than sibling-unit/group spacing through stable semantic layout
markers; exact references wrap and remain unchanged; REWORK-3 section order and
null Business Impact/Decision/Recommendation boundaries remain intact.

Run focused rendering tests, full frontend regression, full Python regression,
TypeScript, production build, `git diff --check`, and exact two-path Scope Guard.
Product Owner acceptance remains pending/not ready. Do not create TASK-0131.

## REWORK 4 - Semantic Color Hierarchy

Product Owner acceptance requires a restrained semantic color hierarchy on the
REWORK-3 structured presentation. This rework is presentation-only and must not
change transported values, layout geometry, parsing, authority, readiness, risk,
or provenance semantics.

### Color Contract

- Structural/data labels use the existing MUI `info.main` theme token. This
  includes labels such as Source, Status, Reason, Completeness, Finding, Asset
  context, Evidence, Inputs, Threat intelligence, CVE/CVSS fields, CIA fields,
  Business service, Environment, Service criticality, Provenance, and Missing
  requirements. Presence alone never gives these labels success/warning color.
- Exact positive status values `READY`, `AVAILABLE`, and `RESOLVED` use
  `success.main`.
- Exact incomplete/unavailable status values `UNAVAILABLE`, `NOT_FOUND`,
  `NOT_EVALUATED`, and `UNKNOWN` use `warning.main`.
- Genuine surfaced failures use existing `error.main`/MUI error presentation;
  technical severity alone must never produce error coloring.
- Ordinary values, sources, identifiers, URLs, and provenance references remain
  neutral using `text.primary` or `text.secondary`.
- Status text remains explicit; color is supplementary and never the only state
  signal. Section headings retain their existing hierarchy and are not globally
  recolored.

Use a small deterministic presentation helper at the component boundary to map
only approved exact status strings to theme tokens. Do not normalize, coerce, or
infer status. Do not hard-code colors or modify global theme tokens.

### Layout and Authority Preservation

Preserve every REWORK-3 label/value block, Stack spacing, grouped input/fact/
effect presentation, wrapping-safe long references, semantic section order,
loading/error/action behavior, and exact authoritative value. Do not redesign the
shell, navigation, Findings geometry, dashboard, Risk Manager, or global theme.
Preserve `business_impact = null`, `decision = null`, empty recommendations, all
TASK-0127/0128/0129 and TASK-0130 authority, and frozen legacy/AI boundaries.

### Tests and Validation

Focused rendering tests must verify semantic theme intent without pixel or raw
computed-color assertions: structural labels declare `info.main`; positive and
unavailable status values declare `success.main` and `warning.main`; genuine
error presentation remains error semantic; neutral references do not acquire
status colors; visible status text remains present; REWORK-3 structure, spacing,
exact values, wrapping, and section order remain intact. Tests must also preserve
the null Business Impact boundary and unchanged behavior.

Run focused rendering tests, full frontend regression, full Python regression,
TypeScript, production build, `git diff --check`, and exact two-path Scope Guard.
Product Owner acceptance remains pending. Do not create TASK-0131.

## REWORK 3 - Structured Authority Presentation Spacing

Product Owner acceptance requires every authoritative datum to render as an
immediately distinguishable label/value unit. This rework is presentation-only
and limited to the existing Finding Risk Context section and its focused tests.
It must not change, derive, normalize, omit, reorder semantically, or fabricate
any transported authority value.

### Layout Contract

Use existing MUI layout primitives, primarily nested `Stack` structures with
consistent responsive `spacing`, to establish:

- label on its own line;
- value on its own line directly beneath the label;
- source/provenance reference in the same value block where applicable;
- clear vertical separation before the next label/value unit;
- long references wrapping within their own block without clipping or document
  overflow.

Do not use arbitrary `<br />`, whitespace characters, negative margins,
transforms, clipping, prose punctuation, or inline concatenation as layout.
Existing colors, typography hierarchy, loading/error behavior, action behavior,
and semantic section order remain intact.

### Required Structured Regions

Apply the same maintained hierarchy to source facts, canonical Asset Context,
Business Context facts, Business-Impact Readiness and missing requirements,
Service Impact Profile, Technical Effect, Business-Impact Classification
Readiness, Threat Intelligence facts and provenance, Correlation, Evidence,
grouped input references, assessment/evidence missing requirements, Finding Risk
Priority, priority provenance, considered evidence, and referenced inputs.

Repeated inputs render under one `Inputs` group with separated label/value
children. Repeated facts/effects/requirements use stable authoritative keys,
never array indexes when an authoritative identity exists. Long source,
evidence, and input references must use wrapping-safe styling such as
`overflowWrap: "anywhere"` on their own value Typography/block.

Dense forms such as `Correlation: available. Source: ...`, `Input: ...`, or
`Readiness source: ...; completeness ...` are prohibited. The UI must continue
to distinguish Business Context, Service Impact Profile, Technical Effect,
readiness, and actual Business Impact; readiness must never look like an impact
result.

### Tests and Preservation

Focused rendering tests must assert structural label/value separation and
non-concatenation for representative source/provenance, Correlation, Evidence,
Inputs, Business Context, both readiness results, Service Impact Profile,
Technical Effect, priority, and missing requirements. Assert wrapping authority
on long references and absence of `<br>` elements. Preserve truthful values,
DistCC UNAVAILABLE behavior, `business_impact = null`, `decision = null`, empty
recommendations, all parser/API/backend authorities, TASK-0127/0128/0129,
REWORK-1/2 invariants, and frozen legacy/AI boundaries.

Run focused section tests, full frontend regression, TypeScript, production
build, Python regression, `git diff --check`, and exact two-path Scope Guard.
Product Owner acceptance remains pending. Do not create TASK-0131.

## REWORK 2 - Immutable CIA and Runtime Version Authority

Architect review freezes every passing TASK-0130 and REWORK-1 boundary. REWORK 2
is limited to the two reproduced authority defects and their focused regression
tests. It does not extend Business Impact capability.

### Immutable Technical Effect CIA Invariant

`FindingTechnicalEffect` must independently validate its exact supported CVSS
version/vector with the existing strict parser and require its stored
confidentiality, integrity, and availability levels to equal the deterministic
vector derivation. This invariant belongs to the immutable value boundary and
must not depend only on `FindingTechnicalEffectService.project()` or the later
Classification Readiness gate.

Direct construction is rejected deterministically when the vector says
`C:N/I:L/A:H` but the supplied levels are `HIGH/HIGH/NONE`. Valid exact
constructions, service projection, multiple-CVE separation, provenance, mixed
CVE fail-closed behavior, and readiness gating remain unchanged. No second
parser, mapping, enum, or effect model may be introduced; reuse the existing
strict validation/derivation authority in `finding_technical_effect.py`.

### Frontend Exact CVSS Version Runtime Type

The runtime parser must require `typeof cvss_version === "string"` before any
membership, prefix, source-binding, or vector validation. Only exact string
values `"3.0"` and `"3.1"` are accepted. `String(...)`, coercion,
normalization, fallback, or joint downstream/source mutation must never turn a
numeric or other runtime value into valid authority.

Starting from an otherwise authoritative source-bound payload, string `"3.1"`
remains accepted. Numeric `3.1` is rejected when present in the Technical
Effect, and remains rejected when the TI/CVSS source version plus Technical
Effect and Classification Readiness copies are all jointly changed to numeric
`3.1`. The existing exact source-snapshot and CIA derivation checks remain
binding.

### Required Focused Tests

- direct immutable construction rejects vector `C:N/I:L/A:H` with supplied
  `HIGH/HIGH/NONE`;
- direct immutable construction accepts exact derived `NONE/LOW/HIGH`;
- normal service-produced effects remain valid;
- frontend accepts exact string `"3.1"` in an otherwise valid bound snapshot;
- frontend rejects numeric `3.1` in a Technical Effect;
- frontend rejects jointly numeric `3.1` across authoritative TI, Technical
  Effect, and Classification Readiness;
- existing strict-vector, provenance, mixed-CVE, API/source-binding, DistCC,
  null Business Impact, and TASK-0127/0128/0129 regressions remain green.

### Preservation and Validation

Run focused Technical Effect and frontend parser tests, full Python and frontend
regressions, TypeScript, production build, `git diff --check`, and an exact
four-path Scope Guard. Preserve `business_impact = null`, `decision = null`,
empty recommendations, Product Owner acceptance PENDING, all frozen legacy/AI
boundaries, and do not create TASK-0131.

## REWORK 1 - Technical Effect Source Authority

Architect review freezes the approved BusinessImportance, ServiceImpactProfile,
strict JSON authority, profile resolution, null Business Impact, DistCC,
UNAVAILABLE, legacy isolation, and TASK-0127/0128/0129 behavior. REWORK 1 is
limited to strict CVSS source validation and end-to-end Technical Effect
provenance/snapshot binding.

### Strict CVSS v3 Authority

No strict CVSS parser is present in the repository, declared Python
dependencies, or installed environment. Adding a dependency would broaden the
approved authority and is unnecessary for the finite supported contract.
Implement one internal deterministic parser in
`application/finding_technical_effect.py` with this complete grammar:

- prefix/version pairs are exactly `3.0` plus `CVSS:3.0/`, or `3.1` plus
  `CVSS:3.1/`;
- exactly one of each base metric is required: `AV`, `AC`, `PR`, `UI`, `S`,
  `C`, `I`, `A`;
- allowed values are `AV=N|A|L|P`, `AC=L|H`, `PR=N|L|H`, `UI=N|R`,
  `S=U|C`, and `C|I|A=N|L|H`;
- tokens may be reordered; empty, malformed, duplicate, missing, unknown, or
  invalid tokens fail closed;
- only `C`, `I`, and `A` produce `NONE`, `LOW`, or `HIGH`; all other metrics
  validate source authority only.

No normalization, case conversion, version reconciliation, score fallback, or
partial C/I/A pseudo-vector is permitted.

### Technical Effect Provenance

An AVAILABLE `FindingTechnicalEffect` is immutable and contains:

- `finding_id`;
- `cve_identifier`;
- `cvss_version` matching the vector prefix;
- exact original `cvss_vector`;
- confidentiality, integrity, and availability `TechnicalEffectLevel`;
- `provenance: ExplanationProvenance` copied unchanged from the authoritative
  CVSS `IntelligenceFact`;
- timezone-aware, non-null `observed_at` copied unchanged from that fact.

`provenance.source_type` must be exactly `nvd`; both provenance fields are
non-empty under the existing Explainability invariant. An effect lacking any
identity, version, vector, provenance, or observation time cannot be AVAILABLE
or satisfy classification readiness. Projection-level deterministic provenance
remains `finding_technical_effect` with the approved status/finding reference.

### Mixed Applicable-CVE Policy

Each applicable CVE is evaluated independently and retains its own vector,
timestamp, and provenance. Duplicate CVE identities are controlled integrity
failures. If every applicable relationship is supported and authority-complete,
the projection is AVAILABLE with separate ordered effects. If any applicable
relationship is unsupported, malformed, or provenance-deficient, the projection
is UNAVAILABLE with a CVE-specific missing requirement. Other valid effects may
be retained only as explicit partial authority; they cannot be aggregated,
reclassified, interpreted as NONE/LOW, or make Classification Readiness READY.
A non-applicable relationship never produces an effect; with no complete
applicable relationship the projection is UNAVAILABLE.

### Classification Readiness Gate

READY requires an AVAILABLE Technical Effect projection and independently
validates every included effect's finding/CVE identity, exact supported
version/vector pair, NVD provenance, source reference, timezone-aware
`observed_at`, and unique CVE identity. Any partial or deficient projection is
UNAVAILABLE and preserves exact missing requirements plus valid partial
authority. Business Impact remains null; no impact matrix, score, band, or
narrative is authorized.

### API Source Binding

Each serialized effect contains `finding_id`, `cve_identifier`, `cvss_version`,
`cvss_vector`, technical C/I/A values, `observed_at`, `source_type`, and
`source_reference`. Together, finding+CVE+version+vector+provenance+timestamp form
the minimal binding identity to the existing transported TI relationship and
its CVSS fact. Classification readiness carries exactly the same effect objects,
not a lossy reconstruction.

### Frontend Snapshot Binding

The parser must locate exactly one applicable transported TI relationship for
each effect CVE and require its available CVSS fact to match effect finding,
CVE, metadata version, vector, provenance source type/reference, and
`observed_at` exactly. It independently parses the strict vector grammar and
verifies derived C/I/A levels. It then requires Classification Readiness effects
to equal the already source-bound Technical Effect projection. Duplicate or
unmatched TI/effect identities, altered values, provenance, timestamps, or CIA
levels fail closed even if both effect copies were changed together.

AVAILABLE effects reject missing/null/invalid timestamps or provenance.
UNAVAILABLE partial effects, when present, must individually remain valid and
source-bound and never promote the result to READY.

### Required Adversarial Tests

Backend/application tests cover both version mismatches; valid matching v3.0
and v3.1; each missing required metric; unknown, invalid, duplicate, malformed,
and empty tokens; reordered complete vectors; absent timestamps; missing or
wrong provenance source type/reference; classification rejection of deficient
effects; valid complete effects; multiple separate CVEs; duplicate CVEs; and
mixed valid plus malformed/unsupported applicable CVEs.

API tests prove version, vector, explicit provenance, and timestamp survive
serialization and that business impact, decision, and recommendations remain
null/null/empty.

Frontend tests start with a valid source-bound TI/CVSS snapshot and independently
tamper CVE, version, vector, C/I/A, source type/reference, timestamp, and effect
identity. They also cover null timestamp, unmatched effects, duplicate effects,
readiness-copy tampering, valid exact binding, and multiple CVEs without
cross-source mixing.

### Preservation and Validation

All original TASK-0130 prohibitions remain binding. Execute focused Technical
Effect, Classification Readiness, API, parser, and transport tests; TASK-0127,
TASK-0128, and TASK-0129 regressions; full Python and frontend suites;
TypeScript; production build; `git diff --check`; and exact nine-path Scope
Guard. Product Owner acceptance remains pending. Do not create TASK-0131.

## Objective

Extend the existing Finding Risk Context with authoritative service-level CIA
business importance, a provenance-preserving technical-effect projection, and a
distinct downstream Business-Impact Classification Readiness result.

Business Impact means the expected organizational consequence if the specific
finding is successfully exploited under the currently supported,
evidence-backed technical scenario. TASK-0130 does not calculate or classify
that consequence. `business_impact` remains `None`/`null`.

## Semantic Separation

The following authorities are distinct and never substitutable:

- `AssetCriticality`
- `ServiceCriticality`
- `DecisionPriority` / `FindingRiskPriority`
- technical severity, exploitation likelihood, CVSS score
- `BusinessImportance`
- future Business Impact

No technical signal, priority, or existing criticality may populate CIA
business importance.

## BusinessImportance Authority

Create a new immutable enterprise-context enum `BusinessImportance` because no
existing type has the approved CIA business-importance semantics. Exact values:

- `CRITICAL`
- `HIGH`
- `MEDIUM`
- `LOW`

Runtime values must be actual `BusinessImportance` members inside the domain.
Strings, other enums, coercion, case normalization, fallback, and matching-label
substitution are invalid.

## ServiceImpactProfile Domain Contract

Create an immutable `ServiceImpactProfile` containing exactly:

- `canonical_asset_id: str`
- `business_service: str`
- `confidentiality_importance: BusinessImportance`
- `integrity_importance: BusinessImportance`
- `availability_importance: BusinessImportance`
- `source_reference: str`

All strings are non-empty and must already be canonical: leading/trailing
whitespace is rejected rather than normalized. All three CIA dimensions are
mandatory. The source reference applies to every profile fact and must survive
resolution, readiness, API serialization, and frontend rendering.

## Strict JSON Authority

The configured Product-Owner-controlled JSON source has exactly this schema:

```json
{
  "serviceImpactProfiles": [
    {
      "canonicalAssetId": "asset-id",
      "businessService": "Service name",
      "confidentialityImportance": "HIGH",
      "integrityImportance": "CRITICAL",
      "availabilityImportance": "CRITICAL",
      "sourceReference": "authoritative-system:record-id"
    }
  ]
}
```

The root and record key sets are exact. Values are not trimmed or converted.
Unknown keys, missing keys, non-string values, whitespace-padded values,
invalid enums, duplicate canonical assets, duplicate exact records, or
conflicting records fail closed with a dedicated controlled data error. An
unconfigured source or absent canonical-asset record returns truthful absence.
The source path is configured by `SERVICE_IMPACT_PROFILE_PATH`; no profile data
is committed by this task.

## Finding Profile Resolution

Create an immutable finding-scoped resolution with states:

- `RESOLVED`
- `NOT_FOUND`
- `MISSING_CANONICAL_ASSET`

Resolution consumes the existing TASK-0129 business-context resolution and
queries by canonical asset, never by service name alone. A RESOLVED profile must
match both `canonical_asset_id` and the exact `business_service` snapshot from
`AssetBusinessContext`. Any mismatch raises a dedicated integrity error. Missing
TASK-0129 context, duplicate/conflicting source data, and missing records are not
silently normalized or inferred.

## Technical-Effect Authority

Create an immutable application projection from the existing authoritative
finding Threat Intelligence snapshot. It is technical effect, not business
importance or Business Impact.

Supported policy is deliberately finite:

- only applicable TI relationships with available NVD CVSS facts;
- only CVSS versions `3.0` and `3.1`;
- vector prefix must exactly match the declared version;
- exactly one each of `C`, `I`, and `A` must exist;
- their exact metric values are `N`, `L`, or `H`, projected to a new technical
  `TechnicalEffectLevel.NONE`, `.LOW`, or `.HIGH`;
- duplicated, missing, malformed, conflicting, or unsupported metrics fail
  closed and are enumerated as missing/unsupported requirements;
- CVSS base score, severity, EPSS, KEV, vendor severity and exposure never
  establish technical effect.

Each immutable effect item contains `finding_id`, `cve_identifier`,
`cvss_version`, the three technical effect levels, original vector,
`source_reference`, and `observed_at`. Preserve the original CVSS fact
provenance and observation time exactly. Multiple applicable CVEs remain
separate effect items; no aggregation or worst-case selection is performed.

The finding projection exposes `AVAILABLE` or `UNAVAILABLE`, exact effect items,
missing requirements, source references, and existing Explainability
completeness/provenance. It is AVAILABLE only when at least one applicable
effect exists and every applicable relationship is supported and proven.
Unsupported CVSS v2/v4, missing CVSS, non-applicable TI, ambiguity, or malformed
vectors remain UNAVAILABLE. No LLM or provider call is permitted.

## Business-Impact Classification Readiness

Create a distinct immutable result; do not repurpose TASK-0129
`BusinessImpactReadiness`.

Schema:

- `finding_id: str`
- `status: BusinessImpactClassificationReadinessStatus`
- `reason: str`
- `business_facts: tuple[BusinessContextFact, ...]`
- `service_impact_facts: tuple[ServiceImpactFact, ...]`
- `technical_effects: tuple[FindingTechnicalEffect, ...]`
- `missing_requirements: tuple[str, ...]`
- `source_references: tuple[str, ...]`
- `completeness: ExplanationCompleteness`

States are exactly `READY` and `UNAVAILABLE`. READY requires:

1. TASK-0129 business context and Business-Impact Readiness are READY and
   mutually consistent.
2. Service Impact Profile is RESOLVED for the same finding, canonical asset and
   exact business service.
3. All three CIA BusinessImportance facts are present, runtime-valid and proven.
4. The technical-effect projection is AVAILABLE, contains at least one valid
   effect, and preserves CVSS provenance and `observed_at`.
5. All finding/source snapshots agree.
6. Result provenance is exactly
   `business-impact-classification-readiness:ready:<finding_id>` with source type
   `business_impact_classification_readiness` and completeness `AVAILABLE`.
7. `missing_requirements` is empty.

UNAVAILABLE preserves every valid partial fact/effect and provenance, reports
exact missing or unsupported requirements, uses non-AVAILABLE completeness and
deterministic result provenance
`business-impact-classification-readiness:unavailable:<finding_id>`. It never
creates an impact score, band, narrative, consequence, decision, or
recommendation.

## Finding Risk Context Integration

Extend the existing projection with:

- `service_impact_profile`
- `technical_effect`
- `business_impact_classification_readiness`

Read each configured authority once and pass unchanged snapshots downstream.
Enforce finding, canonical-asset, business-service, fact, provenance, vector and
observation-time equality. The existing TASK-0127/0128/0129 fields and refusal
semantics remain unchanged. `business_impact`, `decision`, and recommendations
remain null/null/empty.

## API and Frontend

Serialize the exact resolution, profile facts, technical effects, missing
requirements, completeness, source references, result provenance and
`observed_at`. The parser rejects contradictory identities, values, provenance,
vectors, timestamps, readiness states, missing/duplicate/additional facts, or a
non-null Business Impact.

Within the existing Finding Risk Context section, render Service Impact Profile,
Technical Effect and Classification Readiness as sourced facts and truthful
missing requirements. Explicitly state that readiness is not Business Impact
and no classification has occurred. Do not populate Risk Manager, Executive, or
Decision UI.

## DistCC Acceptance

No Service Impact Profile record is authorized for
`asset-lab-metasploitable2-001`. The live DistCC finding must therefore remain:

- Service Impact Profile: `NOT_FOUND`
- Business-Impact Classification Readiness: `UNAVAILABLE`
- `business_impact = null`

Its NVD CVSS v3 technical effect may be shown only when the real authoritative
TI snapshot supplies a supported vector and provenance. That technical effect
must not create business importance or Business Impact.

## Focused Adversarial Tests

Tests must prove at least:

1. Domain immutability and exact BusinessImportance runtime types.
2. No AssetCriticality, ServiceCriticality, DecisionPriority or strings can
   substitute for BusinessImportance.
3. Strict JSON root/record schema, exact strings/enums, no trimming, and
   controlled unreadable/malformed/duplicate/conflicting behavior.
4. RESOLVED/NOT_FOUND/MISSING_CANONICAL_ASSET and canonical/service mismatch
   integrity failure.
5. Valid CVSS 3.0 and 3.1 C/I/A projection with exact provenance and
   `observed_at`.
6. Unsupported v2/v4, prefix/version mismatch, missing/duplicate/unknown C/I/A,
   unavailable CVSS, non-applicable TI and multiple-CVE partial failure remain
   UNAVAILABLE.
7. Base score, severity, EPSS, KEV, vendor severity, exposure, risk priority,
   AssetCriticality and ServiceCriticality cannot create technical effect or
   BusinessImportance.
8. READY requires all business/profile/effect gates, matching snapshots,
   complete provenance, AVAILABLE completeness and no missing requirements.
9. Every missing, contradictory, stale, unproven, unsupported or ambiguous
   input produces truthful UNAVAILABLE.
10. API and frontend preserve exact facts, provenance, timestamps and refusal.
11. DistCC remains profile NOT_FOUND, classification readiness UNAVAILABLE and
    Business Impact null without a configured record.
12. Legacy BusinessContext/BusinessImpact, Decision, Recommendation, AI,
    Risk Manager, Executive and mock paths are never invoked.
13. Full TASK-0127, TASK-0128 and TASK-0129 behavior remains intact.

## Validation

Run focused domain/application/API/frontend tests, full Python and frontend
regressions, TypeScript, production build, `git diff --check`, and an exact
22-path Scope Guard. Generic green validators do not override a provenance,
snapshot, truthfulness, or semantic-boundary defect.

## Product Owner Gate

Product Owner browser acceptance remains mandatory. TASK-0130 must remain READY
after implementation review until live acceptance. No TASK-0131 may be created.
