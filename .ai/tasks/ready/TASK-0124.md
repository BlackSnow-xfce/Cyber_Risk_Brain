---
task_id: TASK-0124
phase: POST-MVP Threat Intelligence
allowed_scope: frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.css, frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.test.tsx
prohibited_actions: api_app.py, application/**, core/**, aidp_orchestration/**, runtime/**, .env, frontend/src/styles/global.css, frontend/src/platform/**, frontend/src/components/topbar/**, frontend/src/workspaces/registry/**, frontend/src/workspaces/threat-intelligence/ThreatIntelligenceWorkspace.tsx, frontend/src/workspaces/threat-intelligence/ThreatIntelligenceLayout.tsx, frontend/src/workspaces/threat-intelligence/navigation.ts, frontend/src/workspaces/threat-intelligence/index.ts, frontend/src/workspaces/threat-intelligence/ThreatIntelligence.ts, frontend/src/workspaces/threat-intelligence/ThreatIntelligenceApiClient.ts, frontend/src/workspaces/threat-intelligence/ThreatIntelligenceApiClient.test.ts, frontend/src/workspaces/threat-intelligence/ThreatIntelligenceWorkspace.test.tsx, frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.tsx, frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceEnvironmentPage.tsx, frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceExplorerPage.tsx, frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceExplorerPage.test.tsx, frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligencePageHeader.tsx, frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceResult.tsx, frontend/src/workspaces/threat-intelligence/pages/IntelligenceStateCard.tsx, frontend/src/workspaces/threat-hunter/**, frontend/src/workspaces/soc/**, frontend/src/workspaces/executive/**, frontend/src/workspaces/incident-response/**, frontend/src/workspaces/risk-manager/**, frontend/src/workspaces/administrator/**, backend/**, provider/**, .ai/references/TASK-0122-product-owner-mockup.png, .ai/references/TASK-0123-product-owner-mockup.png, .ai/references/TASK-0124-product-owner-mockup.png, .ai/tasks/done/TASK-0122.md, .ai/tasks/done/TASK-0123.md, .ai/tasks/**/TASK-0125.md
validation_requirements: frontend tests, typescript, production build, git diff --check
product_owner_gate: true
---
# TASK-0124 - Threat Intelligence Dashboard Visual Unification

Status: READY / REWORK 1
Reviewer: Architect
Architect Task-Specification Review: PASS / APPROVED (REWORK 1)
Architect Implementation Review: PENDING (REWORK 1)
Product Owner Live Acceptance: NOT READY

Historical implementation review: FAIL / REWORK REQUIRED (original TASK-0124)

## Objective

Reconstruct the Threat Intelligence overview in the approved PredatorAI design
language and measured hierarchy of the immutable Product Owner PNG. The image is
visual authority only. Its illustrative security values are not product data.
The existing real Our Environment capability must be reused and preserved rather
than copied or replaced.

## Immutable Visual Authority

- Repository path: `.ai/references/TASK-0124-product-owner-mockup.png`.
- Absolute path: `D:/CyberRiskBrain/.ai/references/TASK-0124-product-owner-mockup.png`.
- SHA-256: `1ae207282bfa5f23e517f9230cc4ced073d5e1d82e5b862ec38563464cdcee87`.
- Dimensions: `1402 x 1122` pixels.
- File size: `1,446,764` bytes.
- The PNG is immutable and is the sole authority for overview composition,
  hierarchy, proportions, spacing, surfaces and visual density.
- The incorrect path `D:/CyberRiskBrain.ai/references/...` is not authoritative.
- Existing Platform Sidebar, Topbar and WorkspaceSwitcher are frozen. Source
  coordinates containing the captured shell must be normalized into the real
  current Main track; the shell must not be altered to match the capture.

## Measured Reference Region Contract

Coordinates are source-PNG pixels. Outer region bounds tolerate at most `6px`,
internal gaps/padding at most `4px`, borders/radii and typography at most `1px`
at `1402 x 1122` and 100 percent zoom. Dynamic truthful text may differ, but
geometry, hierarchy and style remain binding.

| Region | Source bounds / measurement | Binding implementation |
| --- | --- | --- |
| Full canvas | `x 0-1401`, `y 0-1121` | Dark navy Enterprise viewport |
| Captured Sidebar | `x 0-182`, full height | Context only; current 164px Platform contract is frozen |
| Captured Topbar | `x 183-1401`, `y 0-68` | Context only; existing Topbar is frozen |
| Main visual area | approximately `x 202-1384`, `y 69-1098` | Page-local overview fills available Main width |
| Main horizontal inset | about `19px` left and `17px` right inside captured Main | Compact content inset without shell overlap |
| Intelligence Snapshot | `x 203-1384`, `y 69-231`, about `1182 x 163` | One bordered top region with five compact instruments |
| Snapshot label | approximately `x 219-354`, `y 91-101` | Purple uppercase 12px-class overline |
| Snapshot cards | `y 113-217`, about `105px` high | Five compact cards in one row |
| Relevant Findings | `x 217-415`, about `199px` wide | Real findings-derived state only |
| Active Campaigns | `x 429-632`, about `204px` wide | Truthful unavailable state |
| Threat Actors | `x 646-869`, about `224px` wide | Truthful unavailable state |
| New IOCs | `x 882-1105`, about `224px` wide | Truthful unavailable state |
| High Risk CVEs | `x 1118-1321`, about `204px` wide | Truthful unavailable state; never infer from vendor severity |
| Snapshot card gaps | about `13-14px` | Uniform compact separation |
| Our Environment | `x 203-951`, `y 245-672`, about `749 x 428` | Dominant real findings/search panel |
| Environment search | approximately `x 220-841`, `y 322-358` | Existing search semantics reused |
| Environment filter/action area | approximately `x 859-933`, `y 322-358` | No fake filtering capability; truthful navigation/action only |
| Environment results | approximately `x 220-934`, `y 374-623` | Real Finding ID, title, asset, source and vendor severity only |
| Threat Landscape | `x 965-1384`, `y 245-672`, about `420 x 428` | Geographic/map geometry with explicit unavailable state only |
| Landscape map stage | approximately `x 983-1366`, `y 316-480` | No colored geography, actors or activity without backend data |
| Intelligence Feeds | `x 203-775`, `y 688-1098`, about `573 x 411` | Feed-list geometry with Not connected state; no illustrative feed rows |
| Recent Intelligence | `x 791-1384`, `y 688-1098`, about `594 x 411` | Recent-list geometry with Unavailable state; no illustrative entries |
| Major column gap | approximately `14-16px` | Consistent dense grid separation |
| Panel border | approximately `1px` | Thin blue-gray border |
| Panel radius | approximately `14px` outer; `10-11px` inner cards | Compact Enterprise radius language |
| Panel inset | approximately `16-18px` | Dense header and content padding |
| Primary background | near `#010a16` / deep navy | Match approved dark family |
| Panel surface | near `#09131f` | Subtle elevation |
| Inner card surface | near `#101a28` | Compact snapshot and list-stage elevation |
| Accent | PredatorAI purple | Overlines, selected/available accents only |
| Typography | overlines `11-12px`; labels/body `11-13px`; values `27-29px`; panel titles `12-14px` | Compact hierarchy without copying illustrative values |

No element may overlap, clip, escape the page-local Main region or create
document-level horizontal overflow. At the reference viewport the snapshot,
middle row and visible lower row must follow the image hierarchy.

## Existing Threat Intelligence Architecture

- `ThreatIntelligenceWorkspace.tsx` resolves the current pathname and renders
  the overview, Explorer or Our Environment within `ThreatIntelligenceLayout`.
- Canonical routes are `/threat-intelligence`,
  `/threat-intelligence/explorer` and `/threat-intelligence/environment`.
- `WorkspaceOutlet` assigns all `/threat-intelligence` paths to
  `WorkspaceId.THREAT_INTELLIGENCE`; it remains unchanged.
- Explorer performs explicit on-demand CVE lookup through
  `getVulnerabilityThreatIntelligence()` and renders returned NVD, CVSS, EPSS,
  CISA KEV and exploitation facts with provenance. It is not an overview feed.
- Finding-scoped intelligence is loaded only through
  `getFindingThreatIntelligence(findingId)` at its existing explicit boundary.
- Our Environment loads canonical `FindingSummary[]` through `getFindings()`.
  Its search matches Finding ID, title, asset, source and vendor severity.
- Selecting a real environment finding navigates to
  `/findings?findingId=<encoded-id>&focus=threat-intelligence`.
- Deep links, Back/Forward and URL authority remain owned by existing routing.

## Truthfulness Matrix

| Mockup region | Authority classification | Allowed productive representation |
| --- | --- | --- |
| Relevant Findings | A - `GET /api/findings` via `getFindings()` | Loading, error, empty or exact loaded collection count |
| Our Environment rows/search | A - canonical `FindingSummary[]` | Existing search and exact ID/title/asset/source/vendor severity; canonical finding deep link |
| CVE lookup | A only after explicit Explorer request | Preserve Explorer route; do not aggregate into overview |
| Finding-scoped TI | A only after explicit finding selection | Preserve existing route and client; do not prefetch or infer overview values |
| Active Campaigns | B | `Not connected` / `Unavailable`; no count or trend |
| Threat Actors | B | `Not connected` / `Unavailable`; no identities or count |
| New IOCs | B | `Not connected` / `Unavailable`; no indicators or count |
| High Risk CVEs | B for overview aggregation | `Unavailable`; never derive from finding severity or text |
| Threat Landscape | B | Preserve map/panel geometry with neutral unavailable stage; no geography or activity |
| Intelligence Feeds | B | Preserve list-panel geometry with `Not connected`; no feed names, rows or timestamps |
| Recent Intelligence | B | Preserve list-panel geometry with `Unavailable`; no entries, severity or timestamps |
| Trends and time comparisons | B | Never render direction, percentage, series or relative-time claim |

The illustrative mockup values `52`, `18`, `23`, `143`, `31`, its sample
findings, actors, campaigns, IOCs, CVEs, geography, feeds, recent intelligence,
severity distribution, trends and timestamps are prohibited product data.

## Reuse Boundary for Our Environment

`ThreatIntelligenceEnvironmentPage.tsx` may be minimally refactored to expose a
single reusable findings/search presentation used by both the existing page and
the overview. The existing page remains the authority for query normalization,
field matching, loading, error, empty, no-match and canonical finding navigation.
The overview must not create a second parallel search/filter implementation and
must not issue a duplicate findings request merely to feed duplicated widgets.

## Exact Allowed Scope

- `frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.tsx`
  - compose the measured overview and authoritative/unavailable states.
- `frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.css`
  - new page-local visual contract; no global selector authority.
- `frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.test.tsx`
  - focused structure, state, truthfulness and geometry-contract tests.
- `frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceEnvironmentPage.tsx`
  - only extract/reuse the existing canonical findings/search presentation; no
  API, matching, deep-link or standalone-page behavior change.
- `frontend/src/workspaces/threat-intelligence/ThreatIntelligenceWorkspace.test.tsx`
  - update overview expectations and preserve routes and real Our Environment
  behavior after reuse.

No wildcard, implicit path or additional authority is granted.

## Protected Files and Behavior

- Do not change Sidebar, Topbar, WorkspaceSwitcher, WorkspaceRegistry,
  PlatformLayout, WorkspaceOutlet or global CSS.
- Do not change Threat Intelligence Workspace, Layout, navigation, data models,
  API clients, Explorer, intelligence result rendering, shared page header or
  existing backend contracts.
- Do not change TASK-0122, TASK-0123, SOC Dashboard, Threat Hunter Dashboard,
  Explainability, CISO / ISO or any other workspace.
- Do not change backend, provider, runtime, `.env` or any reference image.
- Do not add provider execution, mock data, synthetic security objects or
  frontend inference/business logic.
- Preserve all canonical routes, deep links, Back/Forward and URL authority.
- Preserve WorkspaceSwitcher outside-click/Escape behavior from TASK-0123.

## Acceptance Criteria

- The overview follows the measured snapshot, middle two-panel and lower
  two-panel hierarchy within the specified geometry and tolerances.
- Five snapshot instruments are present in the reference order. Only Relevant
  Findings may show an exact real count after successful `getFindings()` load;
  the other four show truthful unavailable/not-connected states without numbers
  or trends.
- Findings loading, retrieval error and empty collection remain distinct. No
  count is shown during loading/error and empty reports zero only as the exact
  successfully loaded collection size.
- Our Environment reuses the existing canonical implementation and supports the
  same ID/title/asset/source/vendor-severity search, no-match state and exact
  finding deep link. No illustrative row is copied.
- Threat Landscape retains recognizable neutral panel/map-stage geometry but
  shows no highlighted region, legend activity or geography claim.
- Intelligence Feeds and Recent Intelligence retain the mockup panel/list-stage
  geometry while showing explicit truthful unavailable states and no rows.
- No illustrative KPI, security object, severity, time, trend, actor, campaign,
  IOC, CVE, geography, feed or intelligence item enters product code or tests as
  an expected productive value.
- The Overview issues no provider request and does not call either explicit
  threat-intelligence API client automatically.
- Page-local CSS implements measured insets, grid ratios, gaps, borders, radii,
  typography and navy/purple surfaces without changing the Platform shell.
- Overview, Explorer and Our Environment routes, selected workspace, deep links,
  Back/Forward and URL authority remain passing.
- Existing Our Environment standalone behavior and Explorer on-demand lookup
  remain passing and no duplicate environment implementation is introduced.
- Scope Guard reports exactly the five allowed files. Protected files and all
  three visual references remain byte-identical.
- Architect Implementation Review and Product Owner browser acceptance at
  `1402 x 1122` and 100 percent zoom are required before DONE.

## Validation Requirements

- Focused `ThreatIntelligenceOverviewPage` tests for hierarchy, five snapshot
  states, loading/error/empty/real findings, environment reuse and no fake data.
- Existing `ThreatIntelligenceWorkspace` overview, Explorer, Our Environment,
  routing, deep-link and search regressions.
- Existing Threat Intelligence API-client and Explorer tests.
- Existing WorkspaceSwitcher, Sidebar, Topbar and cross-workspace regressions.
- Existing TASK-0122 and TASK-0123 dashboard regressions.
- Full frontend regression.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard for exactly the five allowed paths.
- SHA-256 verification of all protected reference images, including TASK-0124.
- Direct Architect comparison with the PNG and explicit truthfulness review.
- Product Owner browser acceptance at `1402 x 1122` and 100 percent zoom;
  automated DOM/CSS tests do not replace the live visual gate.

## Completion Gates

- Architect Task-Specification Review: `PASS / APPROVED (REWORK 1)`.
- Architect Implementation Review: `PENDING (REWORK 1)`.
- Product Owner Live Acceptance: `NOT READY`.
- No DONE transition and no TASK-0125 creation are authorized.

## Active Execution Authority - REWORK 1

The original TASK-0124 implementation authority above is retained as historical
audit context. REWORK 1 is the only active executable authority and is limited
to correcting the three confirmed numeric visual-contract defects. All passed
data, state, route, truthfulness and preservation behavior is frozen.

### Exact Allowed Scope

- `frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.css`
- `frontend/src/workspaces/threat-intelligence/pages/ThreatIntelligenceOverviewPage.test.tsx`

The existing direct classes for snapshot, environment, landscape, feeds and
recent-intelligence regions make separate geometry possible without changing
`ThreatIntelligenceOverviewPage.tsx`. No other file is authorized.

### Numeric Acceptance Criteria

- Main inset is page-local: left `19px` and right `17px`, each within `+/-4px`.
  A regression to `2px` must fail the geometry contract.
- Snapshot contains five instruments in the existing order with proportional
  target widths `199`, `204`, `224`, `224`, `204`, each within the active visual
  tolerance. Card height remains `105px`, gap remains approximately `13px`, and
  cards plus gaps fit the available snapshot width without clipping or overflow.
  Equal `repeat(5, 1fr)` distribution is prohibited.
- The passed middle row remains approximately `749:420` (`1.78:1`) with a
  `14px` gap. Our Environment remains dominant and Threat Landscape remains the
  narrower panel.
- The bottom row uses an independent layout of approximately `573:594`
  (`0.965:1`) with a `16px` gap and `411px` panel height. It must not inherit the
  middle-row `1.78:1` split and must not overflow or clip.
- A shared page grid may use explicit line segments only when its arithmetic
  independently produces both frozen middle-row and required bottom-row bounds.
- Relevant Findings remains the exact successful `getFindings()` collection
  count. The four other snapshot states and all unavailable panels remain
  truthful and number-free. No mockup value or synthetic security data is added.

### Geometry Contract Tests

- Read the page-local CSS contract and independently assert left/right inset,
  snapshot widths, card height, card gaps, row ratios, row gaps and bottom-panel
  height within the stated tolerances.
- Compute the snapshot width sum plus four gaps against its available inner
  width; reject equal-column substitution and overflow.
- Assert the middle-row `1.78:1` and `14px` contract remains unchanged.
- Assert the bottom-row `573:594`, `0.965:1`, `16px` and `411px` contract is
  distinct from the middle row; a substituted `1.78:1` bottom split must fail.
- These are deterministic CSS-contract tests and do not claim native browser
  pixel measurement. Final visual acceptance remains the Product Owner gate.

### Preservation and Validation

- Freeze the passed middle row, Our Environment reuse, real `getFindings()`
  integration, search, deep link, loading, error, empty and no-match states.
- Freeze truthful Threat Landscape, Feeds and Recent Intelligence states, all
  routing/deep-link contracts, TASK-0122, TASK-0123, WorkspaceSwitcher,
  Sidebar, Topbar, Platform shell, API clients, backend and reference images.
- Run focused Overview geometry/state tests, Threat Intelligence routing and
  deep-link regressions, TASK-0122/TASK-0123 and shell regressions, full frontend
  regression, TypeScript, production build, `git diff --check`, Scope Guard and
  reference-hash verification.
- Architect review must directly verify all three numeric corrections; green
  technical validators alone do not authorize PASS or Product Owner acceptance.
