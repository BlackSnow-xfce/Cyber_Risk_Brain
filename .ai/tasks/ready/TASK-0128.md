---
task_id: TASK-0128
phase: POST-MVP RISK MANAGEMENT
allowed_scope: application/finding_risk_priority.py, application/finding_risk_context.py, application/__init__.py, api_app.py, tests/application/test_finding_risk_priority.py, tests/application/test_finding_risk_context.py, tests/api/test_finding_risk_context_endpoint.py, frontend/src/workspaces/soc/findings/FindingRiskContext.ts, frontend/src/workspaces/soc/findings/FindingRiskContextSection.tsx, frontend/src/workspaces/soc/findings/FindingRiskContextSection.test.tsx
prohibited_actions: analysis/risk_engine.py, core/decision/**, core/predator_engine.py, core/graph.py, application/risk_readiness.py, application/findings_query.py, application/asset_context.py, application/finding_asset_context.py, application/finding_threat_intelligence.py, application/security_observation_correlation.py, application/finding_explanation.py, application/finding_explanation_use_case.py, ingestion/**, infrastructure/**, settings.py, runtime/**, .env, frontend/src/styles/global.css, frontend/src/platform/**, frontend/src/components/topbar/**, frontend/src/workspaces/executive/**, frontend/src/workspaces/threat-hunter/**, frontend/src/workspaces/threat-intelligence/**, frontend/src/workspaces/incident-response/**, frontend/src/workspaces/risk-manager/**, frontend/src/workspaces/administrator/**, backend/**, provider/**, .ai/references/**, .ai/tasks/done/TASK-0122.md, .ai/tasks/done/TASK-0123.md, .ai/tasks/done/TASK-0124.md, .ai/tasks/done/TASK-0125.md, .ai/tasks/done/TASK-0126.md, .ai/tasks/done/TASK-0127.md, .ai/tasks/**/TASK-0129.md
validation_requirements: python tests, frontend tests, typescript, production build, git diff --check
product_owner_gate: true
---
# TASK-0128 - Evidence-Gated Finding Risk Priority

Status: READY
Reviewer: Architect
Architect Task-Specification Review: PASS / APPROVED
Architect Implementation Review: PENDING
Product Owner Live Acceptance: PENDING

## Objective

Extend the existing read-only Finding Risk Context projection with the first
downstream risk-management capability: an evidence-gated, deterministic and
explainable Finding risk priority, or an explicit truthful unavailable result.

The authoritative chain is:

`Finding -> FindingRiskContext -> context readiness + evidence readiness ->
RiskPriority or explicit refusal`.

TASK-0128 does not create a score, business impact, treatment, decision or
recommendation authority. It classifies only a score already produced by the
existing gated `RiskReadinessService` and only when canonical evidence is also
ready.

## Architectural Boundary

- Add `application/finding_risk_priority.py` as a deterministic application
  policy over existing immutable `RiskAssessmentResult` and
  `RiskAssessmentReadinessResult` inputs.
- Reuse the existing, frozen `core.decision.priority_engine.PriorityEngine`
  and `DecisionPriority` band contract. Do not copy its thresholds or introduce
  a competing classifier.
- Keep `RiskReadinessService` as the only permitted technical-score boundary.
- Keep `RiskAssessmentReadinessService` as the canonical-evidence gate.
- `FindingRiskContextUseCase` composes the new result after its existing source
  integrity checks and readiness evaluations.
- The API remains the existing read-only endpoint:
  `GET /api/findings/{finding_id}/risk-context`.
- The frontend renders the transported result and performs no classification,
  scoring or inference.

## Immutable Priority Contract

Define an immutable application contract with:

- `finding_id`;
- status exactly `PRIORITIZED` or `UNAVAILABLE`;
- nullable priority band using the existing values `critical`, `high`,
  `medium`, `low`, `informational`;
- nullable source risk score;
- deterministic reason;
- considered correlation Evidence IDs;
- referenced authoritative input/provenance references;
- explicit missing requirements;
- completeness and provenance identifying the priority policy and Finding.

`PRIORITIZED` requires all of the following:

1. context assessment status is `ASSESSED`;
2. the assessment contains a non-null bounded score;
3. evidence readiness status is `READY`;
4. assessment, evidence readiness and requested Finding IDs match;
5. canonical evidence identifiers and referenced input references are retained.

Otherwise status is `UNAVAILABLE`, band and score are null, and the reason and
missing requirements enumerate the failed gates. Unknown and not-evaluated
states must not be converted to false, zero or a default priority.

## Deterministic Band Authority

- Use `PriorityEngine.calculate(score)` unchanged.
- Existing thresholds remain authoritative through that implementation:
  critical `>= 90`, high `>= 75`, medium `>= 50`, low `>= 25`, otherwise
  informational.
- TASK-0128 must not duplicate these thresholds in production code.
- The output describes Finding risk priority only. It must not create or imply
  a `DecisionResult`, analyst decision, management approval or remediation SLA.

## Fail-Closed Requirements

- `INSUFFICIENT_CONTEXT` always yields priority status `UNAVAILABLE`, null band
  and null score, regardless of vendor severity, CVSS, EPSS or CISA KEV data.
- `INSUFFICIENT_EVIDENCE` always yields `UNAVAILABLE`, even if a score-shaped
  value is supplied by a controlled test fixture.
- Finding-ID, evidence-ID, provenance or source-snapshot mismatch fails closed.
- Empty or inconsistent provenance cannot produce a priority.
- Existing `refusal_reason` remains visible and consistent with priority
  unavailability.
- Business impact, decision and recommendations remain respectively `null`,
  `null` and `[]` in every TASK-0128 result.

## DistCC Acceptance Scenario

Use the configured real Finding
`6d3167e9-002c-4b76-a5a7-ce47f81b78b1` / `CVE-2004-2687` without hard-coded
production branching.

The current authoritative projection resolves Greenbone facts, canonical asset
`asset-lab-metasploitable2-001`, TI and correlation evidence. Exposure,
detection availability, TI match and MITRE tactic remain `NOT_EVALUATED`.
Therefore the expected live result is:

- priority status `UNAVAILABLE`;
- priority band and priority score are null;
- missing risk inputs and evidence requirements remain visible;
- the UI explains that priority was refused because required authoritative
  context is incomplete;
- no critical/high/default priority is inferred from the Finding title,
  Greenbone severity, CVSS, EPSS, CISA KEV or canonical asset alone.

Positive priority behavior is proven with controlled typed unit fixtures whose
inputs are explicitly authoritative. Such fixtures are tests of the policy and
must not become product, runtime or demo data.

## API and UI Contract

- Replace the transport-level `priority: null` placeholder with a typed priority
  projection; do not add a second endpoint.
- Preserve all existing source facts, AssetContext, TI, evidence, readiness and
  refusal response fields.
- Finding Detail renders either the band with its score, evidence references
  and explanation, or an explicit `Priority unavailable` state with missing
  gates.
- The frontend must not sort Findings, calculate thresholds, infer priority or
  populate Risk Manager/Executive dashboards.
- Loading, error, Finding selection reset and stale-request protection from
  TASK-0127 remain unchanged.

## Explicit Non-Goals and Negative Guarantees

- No business-impact calculation or claim.
- No risk treatment, remediation recommendation, ownership assignment, cost,
  effort, risk reduction, confidence, due date, target time or SLA.
- No Decision Engine, `/api/decisions`, `PredatorEngine`, BusinessContextEngine,
  BusinessImpactBuilder, RecommendationEngine or RecommendationBuilder call.
- No LLM/provider call and no AI-generated priority.
- No synthetic enterprise context, mock runtime data or scanner-default
  promotion.
- No compliance, trends, Outlook/email workflow, executive reporting or Risk
  Manager Mission Console data.
- No persistence or mutation of Findings, assets, TI, evidence, incidents,
  hypotheses, risk or decisions.

## Acceptance Criteria

1. The new immutable priority contract has only `PRIORITIZED` and `UNAVAILABLE`
   states and retains deterministic provenance.
2. Priority classification occurs only when both existing readiness gates pass.
3. Exactly one already-gated score is classified through the existing
   `PriorityEngine`; no parallel thresholds or score engine exist.
4. Each boundary score maps to the existing band contract, including exact
   threshold edges.
5. Vendor severity or CVSS alone cannot produce priority.
6. Missing/unknown/not-evaluated context produces explicit unavailability with
   null band and score.
7. Missing/inconsistent evidence or provenance produces explicit unavailability
   or source-integrity failure.
8. Finding, assessment and evidence-readiness IDs must match.
9. The API transports status, nullable band/score, reason, missing requirements,
   evidence IDs, input references and provenance without reinterpretation.
10. Finding Detail visibly distinguishes explainable priority from refusal.
11. Existing `business_impact`, `decision` and `recommendations` remain null,
    null and empty.
12. The real DistCC scenario remains truthful `UNAVAILABLE` while its required
    risk inputs remain `NOT_EVALUATED`.
13. TASK-0127 source integrity, live endpoint, routing and rendering behavior do
    not regress.
14. No protected file or prohibited authority is modified or invoked.

## Required Tests

- Focused application tests for both statuses, every band boundary, both
  readiness gates, ID mismatches, invalid/missing score, provenance retention
  and stable ordering of missing requirements.
- Negative tests proving vendor severity, CVSS, EPSS and CISA KEV cannot bypass
  missing context or evidence.
- Tests proving the classifier is not called when either gate fails and is
  called exactly once when both pass.
- FindingRiskContext integration tests preserving snapshot identity,
  `observed_at`, REWORK-1/2/3 mismatch rejection and all null downstream fields.
- API tests for typed priority transport, real refusal shape, controlled source
  failures and absence of Decision/Recommendation execution.
- Frontend parser/rendering tests for prioritized and unavailable states,
  provenance, missing gates, loading/error lifecycle and stale Finding reset.
- Existing Finding, AssetContext, TI, correlation, Explainability and Incident
  regressions.
- Full Python and frontend regression, TypeScript, production build,
  `git diff --check`, exact-scope guard and direct Architect truthfulness review.

## Protected Authorities

All prohibited paths in metadata are frozen. In particular preserve TASK-0127,
`RiskReadinessService`, `RiskAssessmentReadinessService`, `RiskEngine`,
`PriorityEngine`, `DecisionPriority`, Finding/AssetContext/TI/correlation
authorities, all platform/workspace shells, other workspaces, backend/providers,
runtime, `.env`, Product Owner references and TASK-0122 through TASK-0127.

## Product Owner Live Acceptance

On the real DistCC Finding Detail, load Authoritative Risk Context and verify:

- authoritative facts, TI and correlation evidence remain visible;
- Priority is explicitly unavailable with null band and score;
- all missing inputs and requirements explain the refusal;
- no vendor-severity/CVSS-derived or otherwise fabricated priority appears;
- business impact, decision and recommendations remain unavailable;
- existing Findings navigation, linked incidents and Back/Forward behavior remain
  intact.

No DONE transition is authorized without direct Architect Implementation Review
PASS and Product Owner Live Acceptance PASS.
