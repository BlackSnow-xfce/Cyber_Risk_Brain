---
task_id: TASK-0122
phase: POST-MVP SOC Operations
allowed_scope: frontend/src/platform/layout/PlatformLayout.test.tsx, frontend/src/workspaces/executive/ExecutiveLayout.test.tsx
prohibited_actions: api_app.py, application/**, core/**, frontend/src/components/topbar/WorkspaceSelector.tsx, frontend/src/workspaces/soc/findings/**, frontend/src/workspaces/soc/SOCWorkspace.tsx, frontend/src/workspaces/soc/SOCWorkspaceToolbar.tsx, frontend/src/workspaces/soc/explainability/**, frontend/src/workspaces/soc/dashboard/InvestigationContextGraph.tsx, frontend/src/workspaces/soc/dashboard/InvestigationGraph.tsx, frontend/src/workspaces/soc/dashboard/InvestigationRelationshipGraph.tsx, frontend/src/workspaces/soc/dashboard/InvestigationStateTimeline.tsx, frontend/src/workspaces/soc/dashboard/PredatorAIAnalysisPanel.tsx, frontend/src/workspaces/soc/dashboard/RecommendedInvestigationPanel.tsx, frontend/src/workspaces/soc/dashboard/SOCAnalystCockpit.css, frontend/src/workspaces/soc/dashboard/SOCAnalystCockpit.tsx, frontend/src/workspaces/incident-response/**, frontend/src/workspaces/threat-hunter/**, frontend/src/workspaces/administrator/**, frontend/src/workspaces/executive/ExecutiveDashboardLayout.tsx, frontend/src/workspaces/executive/ExecutiveWorkspace.tsx, frontend/src/workspaces/executive/index.ts, frontend/src/workspaces/executive/navigation.ts, frontend/src/workspaces/executive/pages/ExecutiveAreaPage.tsx, frontend/src/workspaces/executive/pages/ExecutiveOverviewPage.tsx, frontend/src/workspaces/executive/pages/index.ts, frontend/src/workspaces/executive/widgets/AIExecutiveBrief.tsx, frontend/src/workspaces/executive/widgets/BusinessImpact.tsx, frontend/src/workspaces/executive/widgets/EnterpriseRisk.tsx, frontend/src/workspaces/executive/widgets/ExecutiveOverviewPage.tsx, frontend/src/workspaces/risk-manager/**, frontend/src/workspaces/threat-intelligence/**, frontend/src/ui/theme/**, frontend/src/styles/**, aidp_orchestration/**, runtime/**, .env, .ai/references/TASK-0122-product-owner-mockup.png, .ai/tasks/done/TASK-0120.md, .ai/tasks/done/TASK-0121.md, .ai/tasks/**/TASK-0123.md
validation_requirements: frontend tests, typescript, production build, git diff --check
product_owner_gate: true
---
# TASK-0122 - SOC Dashboard Visual Parity

Status: READY / REWORK 13
Reviewer: Architect
Architect Task-Specification Review: PASS / APPROVED (REWORK 13)
Architect Implementation Review: PENDING (REWORK 13)
Product Owner Live Acceptance: FAIL / REWORK REQUIRED

## Historical Audit Record - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

Every section below this heading up to `Rework 7 Binding Visual Reference` is
retained only as immutable task history. It grants no current execution
authority, supplies no active acceptance criterion and supplies no active
validation requirement. If any historical wording conflicts with Rework 7,
the verified PNG and the active Rework 7 sections control without exception.

## Historical Acceptance Criteria - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Use only the existing productive Findings, Finding-to-Incident and Incident Command Center read paths; do not add or change backend, domain, repository or runtime data.
- An active dashboard context must be selected by an explicit existing URL findingId that exactly matches a loaded canonical finding; never label or use the first returned finding as active merely because it is first.
- Without an explicit valid investigation context, show one compact targeted empty state with a real Findings entry point instead of a grid of repetitive unavailable cards.
- Loading and retrieval failures remain visibly distinct and never fall back to mock, synthetic or hard-coded security data.
- For a valid context, render the compact original PredatorAI SOC mockup hierarchy: operational investigation header, situation-oriented status row, compact directed relationship chain, Evidence, Analysis and Recommended Investigation workspace, chronological timeline and prominent Incident Command Center path.
- Project only canonical finding ID, source, title, vendor severity and observed asset identifier from FindingSummary.
- Project only persisted incident identity, relationship role and lifecycle status from the Finding-to-Incident response, and only canonical incident title, description, timestamps, references and activities from the existing Incident Command Center response.
- Do not invent or infer priority, elapsed runtime, attack confidence, potential spread, business impact, MITRE mapping, AI hypothesis, evidence, observations, security conclusions, recommended remediation or compromise from title text, severity, CVE text, counts or relationship presence.
- Omit non-authoritative values or mark the relevant section unavailable without repeating unavailable placeholders across every card; severity must not be presented as priority and timestamps must not be converted into a claimed runtime.
- The directed relationship view may show only relationships established by supplied canonical responses; decorative edges must not assert an unpersisted relationship.
- Evidence and chronological timeline content must come only from persisted references or canonical timestamped incident activities and preserve their supplied ordering/identity semantics.
- Do not synthesize an AI hypothesis or analyst narrative; when no authoritative analysis projection exists, present a truthful unavailable state.
- Recommended Investigation is limited to navigation into existing authoritative Finding, Threat Intelligence, Incident and Command Center read paths and must not claim remediation or evidence.
- When a linked incident exists, provide a prominent action to /incident-response/incidents/<incident-id>/command-center; without one the action is absent or disabled.
- Preserve existing Finding and Threat Intelligence navigation and the URL-authoritative Dashboard, Explainability, Back, Forward, deep-link, refresh and sidebar synchronization delivered by TASK-0121.
- Preserve the defined Explainability unavailable state and do not modify Explainability, SOCWorkspace, Incident Response or routing implementation.
- Opening the dashboard performs no AI provider or model execution and production code instantiates no mock repository.
- Use the existing PredatorAI dark-first design tokens and responsive/reduced-motion behavior; make only the bounded card-density, spacing and hierarchy changes needed for mockup parity, not a broader dashboard redesign.
- Add focused tests for exact findingId selection, invalid/missing/unresolved context, empty/loading/error states, canonical incident and activity projection, Command Center navigation, absence of invented fields/relationships/provider calls, and TASK-0121 routing regression.
- Run focused dashboard and routing tests, the full frontend regression, TypeScript validation, production build and git diff --check; move TASK-0122 to REVIEW only after all validations pass.

## Historical V7 Implementation and Review Evidence - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Architect contract: `architect-task-0122-v7`.
- Execution ID: `58a10c50-3331-49e7-ac80-0ba1ee1880b1`.
- Execution status: `SUCCESS`.
- Scope compliance: `COMPLIANT`.
- Execution commit: `8d1f66b273291be0139cb9f539df8620297e1d84`.
- Review-envelope commit: `0ab00fd5e34f9de352628445deb7df36be198839`.
- Review envelope: `.ai/orchestration/review-inbox/TASK-0122-58a10c50-3331-49e7-ac80-0ba1ee1880b1.json`.
- Consumption state: `REVIEW_PUBLISHED`.
- Persisted validation results: frontend tests PASS, TypeScript PASS,
  production build PASS, and `git diff --check` PASS.
- Product Owner Live Acceptance was subsequently completed with `FAIL / REWORK
  REQUIRED`; no approval or DONE transition is implied.

## Historical Product Owner Live Acceptance Failure - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Result: `FAIL / REWORK REQUIRED`.
- The active investigation view is not reachable through the normal product UI:
  selecting a Finding updates only local Findings state, while Sidebar navigation
  back to Dashboard uses `/` without the selected `findingId`.
- The Dashboard toolbar also presents `Search findings` as an enabled input even
  though its value is fixed and its change handler is a no-op.
- TASK-0122 returns to READY for a separately authorized same-task rework. No DONE
  transition or Product Owner approval is implied.

## Historical Rework 1 Contract - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Contract ID: `architect-task-0122-rework-1`.
- Expected head is bound exactly in the published Architect contract after the
  administrative READY commit is created.
- Preserve all successful V7 evidence; validate this rework independently.
- Make Finding selection URL-authoritative as
  `/findings?findingId=<validated-finding-id>`.
- When Dashboard is selected through the Sidebar from a Finding context, navigate
  to `/?findingId=<validated-finding-id>` so the active investigation is reachable
  without manual URL manipulation.
- Remove or hide the non-functional Dashboard `Search findings` control. Do not
  present it as interactive unless it performs a real canonical Finding selection.
- Add focused coverage for Finding selection and URL synchronization, Findings to
  Dashboard Sidebar navigation, active investigation rendering after navigation,
  browser Back/Forward, direct deep links, Dashboard without `findingId`, and the
  absence of the non-functional search field.
- Do not broaden TASK-0122, introduce synthetic data, or change backend, routing
  architecture, provider execution, completed tasks, or TASK-0123.

## Historical Rework 2 Evidence - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Architect contract: `architect-task-0122-rework-2`.
- Execution ID: `72d50d09-eafa-4594-b5fb-b6d089296446`.
- Execution status: `SUCCESS`.
- Scope compliance: `COMPLIANT`.
- Execution commit: `b5a4b1d19020f692c7f2de5a1abf626383864175`.
- Review-envelope commit: `85beb70e02bf4281894b4eb9c9ca081de5a7d049`.
- Review envelope:
  `.ai/orchestration/review-inbox/TASK-0122-72d50d09-eafa-4594-b5fb-b6d089296446.json`.
- Consumption state: `REVIEW_PUBLISHED`.
- Persisted validation results: frontend tests PASS, TypeScript PASS,
  production build PASS, and `git diff --check` PASS.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED` until the
  repaired user flow passes a new live acceptance. No approval or DONE transition
  is implied.

## Historical Architect Review - Rework 2 - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Result: `FAIL / REWORK REQUIRED`.
- URL history currently changes only `selectedFinding`; dependent Explanation,
  Threat Intelligence and linked-Incident state can remain attached to a different
  Finding after Back or Forward.
- Findings still implicitly select `loadedFindings[0]` without an explicit valid
  URL `findingId`.
- Sidebar transports the query context but does not establish canonical Finding
  validity; tests must not claim a stronger validation boundary.
- Existing tests cover isolated components but not the complete user journey from
  Finding selection through Sidebar Dashboard navigation and browser history.

## Historical Rework 3 Contract - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Proposed contract ID: `architect-task-0122-rework-3`.
- Expected head is bound exactly after this administrative READY transition is
  committed.
- On every URL `findingId` change, including `popstate`, Back and Forward, reset
  all Finding-dependent Explanation, Threat Intelligence, linked-Incident,
  loading, error and derived detail state before using the new canonical Finding.
- Remove implicit first-Finding selection. Without an exact valid URL `findingId`,
  `selectedFinding` remains `null` and no Finding details are presented.
- FindingsWorkspace may write only IDs from its canonically loaded Findings.
  Sidebar transports the URL context without claiming canonical validation;
  Dashboard remains the fail-closed canonical validation boundary.
- An invalid or manipulated `findingId` must not display data belonging to another
  Finding.
- Add a real routed integration test covering Finding selection, Sidebar Dashboard
  navigation, exact active Dashboard context, Back with reset/matching dependent
  detail state, and Forward restoring the exact Dashboard Finding.
- Preserve the accepted visual TASK-0122 implementation and do not broaden product
  scope, lifecycle, data, backend, provider execution or routing architecture.

## Historical Rework 3 Execution Result - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Architect contract: `architect-task-0122-rework-3`.
- Execution ID: `0a8f7308-7ba7-4c62-ad46-f6046f5d5d01`.
- Consumption state: `BLOCKED`.
- Execution status: `TEST_FAILED`.
- Scope compliance: `COMPLIANT`.
- Persisted validation results: frontend tests FAIL (`exit_code=1`), TypeScript
  PASS, production build PASS, and `git diff --check` PASS.
- No execution commit or review envelope was produced. The authorized Rework 3
  product/test changes remain preserved for same-task continuation.

## Historical Rework 4 Contract - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Fix the routed integration test through deterministic router and asynchronous
  synchronization with correct cleanup; do not mask the cause by merely raising
  the timeout.
- Update the seven existing Finding Threat Intelligence tests to provide an
  explicit canonical URL `findingId` before rendering and clean up URL state.
- Do not restore implicit first-Finding selection or change product behavior to
  satisfy obsolete tests.
- Preserve the visual Dashboard implementation and run the complete frontend
  regression, TypeScript, production build, and `git diff --check`.

## Historical Rework 5 Evidence - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Architect contract: `architect-task-0122-rework-5`.
- Contract commit: `80c15f5da917a3f4b57b64b23cc92ec75701ea3b`.
- Execution ID: `b99dde94-09c4-48ef-ab94-01b378bf8d60`.
- Execution status: `SUCCESS`.
- Consumption state: `REVIEW_PUBLISHED`.
- Scope compliance: `COMPLIANT`.
- Execution commit: `c24aa1eeacd15e0b4460fde045975a0abe0826d2`.
- Review-envelope commit: `612d4d8956a8c5c68fcd1f80e7c674f11bc0ef15`.
- Persisted validation results: frontend tests PASS, TypeScript PASS,
  production build PASS, and `git diff --check` PASS.
- Architect Implementation Review: `PASS / APPROVED`.
- Product Owner Live Acceptance remains `PENDING`; no DONE transition or final
  Product Owner approval is implied.

## Historical Product Owner Live Acceptance - Rework 6 - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Result: `FAIL / REWORK REQUIRED`.
- Functional navigation remained accepted, but the rendered dashboard had no
  visual parity with the binding Product Owner mockup.
- The Rework 6 active-investigation cockpit, abstract KPI instruments,
  relationship graph and equal-weight panels are not the visual target for
  Rework 7 and must not be retained as the dashboard composition merely through
  cosmetic adjustment.

## Active Execution Authority - REWORK 7 ONLY

Only the sections from this heading through `Rework 7 Non-Goals` are active
execution authority. The sole visual target is
`.ai/references/TASK-0122-product-owner-mockup.png`. No earlier cockpit,
investigation-graph, KPI or panel requirement is executable in Rework 7.

## Rework 7 Binding Visual Reference

- Immutable repository path:
  `.ai/references/TASK-0122-product-owner-mockup.png`.
- Original filename: `Cybersicherheits-Dashboard mit KI-Agenten.png`.
- Dimensions: `1536 x 1024` pixels.
- Byte size: `1,649,791`.
- SHA-256:
  `af672a97050c5e535199d8d006012cf000e5e7b9b66693a02817c4356eea80df`.
- The image is the sole authority for layout, hierarchy, proportions, spacing,
  density, panel geometry, typography, visual styling and interaction-state
  appearance. Text from previous Rework descriptions cannot override it.
- The reference is read-only implementation input and must not be edited,
  regenerated, optimized, recompressed or replaced.

## Rework 7 Pixel/Region Contract

Coordinates are measured against the full `1536 x 1024` reference canvas and
serve as testable layout anchors rather than permission to copy mock values.

| Mockup region | Reference geometry | Required implementation |
| --- | --- | --- |
| Enterprise Sidebar | `x=0`, `y=0`, `w=164`, `h=1024` | Reconstruct the complete PredatorAI navigation hierarchy, branding, workspace selector and bottom Collapse area using existing real routes. |
| Topbar | `x=164`, `y=0`, `w=1372`, `h=64` | Match Dashboard/Enterprise Workspace title stack, greeting line, period/calendar/refresh controls, Explain This Dashboard, search, notifications, theme control and operator area. |
| Main canvas | `x=164`, `y=64`, `w=1372`, `h=960`; panel content starts at `x=176` | Use the compact navy desktop canvas and the measured panel geometry below. |
| Overall Risk Score | `x=176`, `y=64`, `w=249`, `h=218` | First KPI instrument. Unsupported score remains `Unavailable`. |
| Total Findings | `x=435`, `y=64`, `w=201`, `h=218` | Second KPI instrument using the canonical findings collection count. |
| Active Investigations | `x=646`, `y=64`, `w=185`, `h=218` | Third KPI instrument; no invented active-state definition. |
| Risk Trend | `x=841`, `y=64`, `w=303`, `h=218` | Fourth KPI instrument; unsupported trend remains `Unavailable`. |
| Exposed Assets | `x=1154`, `y=64`, `w=372`, `h=230` | Fifth KPI instrument; unsupported exposure values remain `Unavailable`. |
| Findings by Severity | `x=176`, `y=292`, `w=249`, `h=213` | First secondary panel using only vendor-severity aggregation. |
| Top Risky Assets | `x=435`, `y=292`, `w=252`, `h=213` | Second secondary panel; ranking remains `Unavailable`. |
| AI Insights | `x=697`, `y=292`, `w=241`, `h=213` | Third secondary panel; no provider call, value remains `Unavailable`. |
| AI Agents Status | `x=948`, `y=292`, `w=246`, `h=213` | Fourth secondary panel; agent inventory remains `Not configured`. |
| Recent Decisions | `x=1204`, `y=292`, `w=277`, `h=213` | Fifth secondary panel; use only a proven typed decision read contract, otherwise `Unavailable`. |
| Lower AI Agents area | `x=176`, `y=517`, `w=632`, `h=454` | Reconstruct title/actions, five status summary cells, search/filter row, dense table, pagination and empty/not-configured presentation. |
| Lower selected-agent area | `x=808`, `y=517`, `w=722`, `h=454` | Reconstruct selected-agent header/actions, tabs and the Information, Performance, Recent Activity, Capabilities and Explainability layout. |
| Theme popover state | upper-right overlay in the reference | The theme control and its open popover must visually match this interaction state when explicitly opened; it must not obscure the dashboard by default. |
| Full desktop viewport | complete canvas at `1536 x 1024`, 100% zoom | The principal rows and lower split must be visible with the same overall hierarchy; no active-investigation cockpit may replace them. |

### Measured spacing, surfaces and typography

- Outer content inset: `12px` left of the first panel, `10px` nominal right,
  `0px` above the first row relative to the main-canvas origin.
- Horizontal panel gap: `10px`; first-to-second row gap: `10px`; second-row
  to lower-area gap: `12px`. The lower left/right areas share the split at
  `x=808` and therefore have no inter-panel gutter.
- Panel padding: `12px`; dense table cells: `8px 10px`; compact controls:
  `8px 10px`.
- Panel and control border: `1px`; panel radius: `6px`; compact control radius:
  `4px`.
- Page title: `20px/24px`; greeting and workspace metadata: `9px/14px`;
  panel headings: `11px/16px`; lower-area heading: `16px/22px`; primary KPI:
  `28px/34px` (gauge value `32px/38px`); card labels and metadata: `8px/12px`;
  table header/body: `8px/13px`; buttons/status chips: `8px/12px`.
- Reference color anchors, measured from the verified PNG: shell/topbar
  `#000411` (`rgb(0,4,17)`), sidebar `#010717` (`rgb(1,7,23)`), main canvas
  `#010817` (`rgb(1,8,23)`), panel `#0C1423` (`rgb(12,20,35)`), border
  `#1C212F` (`rgb(28,33,47)`), primary text `#F0F0F0`
  (`rgb(240,240,240)`), secondary text `#989FA5` (`rgb(152,159,165)`),
  purple accent `#5728D2` (`rgb(87,40,210)`), success `#19924A`
  (`rgb(25,146,74)`), warning `#F59D19` (`rgb(245,157,25)`), danger
  `#EF4444` (`rgb(239,68,68)`), information `#0174E1`
  (`rgb(1,116,225)`), disabled `#64748B` (`rgb(100,116,139)`).

### Visual acceptance tolerances

- Sidebar, Topbar and main regions: maximum `+/-4px`.
- Panel positions and panel sizes: maximum `+/-4px`.
- Gaps, padding and internal alignment: maximum `+/-3px`.
- Border thickness and radius: maximum `+/-1px`.
- Font size and line height: maximum `+/-1px`.
- Color anchors: maximum `+/-8` per RGB channel.
- No element may overlap unintentionally, be clipped or fall outside the
  reference viewport. Both five-panel rows and the upper visible portion of
  the lower `632:722` split must be visible at `1536 x 1024`, 100% zoom.
- Truthful dynamic text may differ from the mockup; geometry, hierarchy and
  styling must remain within these tolerances.

## Rework 7 Exact File Authority and Purpose

| Authorized file | Sole permitted purpose |
| --- | --- |
| `frontend/src/platform/layout/PlatformLayout.tsx` | Set the measured Sidebar, Topbar and main-canvas tracks. |
| `frontend/src/platform/navigation/Sidebar.tsx` | Compose the complete reference navigation from existing real destinations. |
| `frontend/src/platform/navigation/Sidebar.css` | Implement measured Sidebar geometry, density, colors and states. |
| `frontend/src/platform/navigation/SidebarSection.tsx` | Render reference section headings without changing route authority. |
| `frontend/src/platform/navigation/SidebarItem.tsx` | Render compact reference navigation items and active states. |
| `frontend/src/platform/navigation/Sidebar.test.tsx` | Verify hierarchy, real routes, active state and collapse behavior. |
| `frontend/src/platform/navigation/Topbar.tsx` | Compose the measured reference Topbar. |
| `frontend/src/platform/navigation/Topbar.css` | Implement measured Topbar geometry and styling. |
| `frontend/src/platform/navigation/Topbar.test.tsx` | Verify controls, truthful identity state and theme-popover behavior. |
| `frontend/src/components/topbar/index.ts` | Export only the concrete Topbar components used by `Topbar.tsx`. |
| `frontend/src/components/topbar/TimeRangeSelector.tsx` | Match the static existing time-range control presentation without inventing data filtering. |
| `frontend/src/components/topbar/TopbarActions.tsx` | Preserve real actions, route Explainability and disable unsupported controls truthfully. |
| `frontend/src/components/topbar/UserMenu.tsx` | Remove fake identity and project trusted session metadata or neutral unavailability. |
| `frontend/src/components/topbar/WorkspaceSwitcher.tsx` | Match the Enterprise Workspace label while preserving existing workspace selection. |
| `frontend/src/workspaces/soc/navigation.ts` | Expose the reference Sidebar hierarchy using existing real routes only. |
| `frontend/src/workspaces/soc/pages/DashboardPage.tsx` | Load existing authoritative collections, project truthful states and compose `EnterpriseSOCDashboard`. |
| `frontend/src/workspaces/soc/pages/DashboardPage.test.tsx` | Verify data projection, states, routes and absence of invented values/provider calls. |
| `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.tsx` | New bounded presentation component for the exact reference regions; replaces the old cockpit only in Dashboard composition. |
| `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.css` | New bounded stylesheet implementing the numerical visual contract. |
| `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.test.tsx` | Verify region order, panel counts, semantic states and layout hooks. |
| `frontend/src/workspaces/soc/SOCWorkspace.test.tsx` | Preserve URL-authoritative Dashboard/Explainability history and deep links. |
| `frontend/src/platform/workspace/WorkspaceOutlet.test.tsx` | Preserve cross-workspace route resolution for existing drill-downs. |

All eight legacy files under `frontend/src/workspaces/soc/dashboard` named in
`prohibited_actions` remain byte-identical. `DashboardPage.tsx` stops composing
`SOCAnalystCockpit`; no legacy cockpit component is adapted, reused as the
visual target or deleted.

## Rework 7 Current-to-Target Gap Matrix

| Mockup region | Current implementation | Required change | Authorized files |
| --- | --- | --- | --- |
| Sidebar | `270/280px`, workspace-local sections and oversized spacing | Reduce to reference proportions and compose the complete Enterprise hierarchy while retaining URL-authoritative routes | `frontend/src/platform/layout/PlatformLayout.tsx`; `frontend/src/platform/navigation/Sidebar.tsx`; `frontend/src/platform/navigation/Sidebar.css`; `frontend/src/platform/navigation/SidebarSection.tsx`; `frontend/src/platform/navigation/SidebarItem.tsx`; `frontend/src/workspaces/soc/navigation.ts`; `frontend/src/platform/navigation/Sidebar.test.tsx` |
| Topbar | Generic MUI row; no greeting; static fake user; several inert buttons | Reconstruct the reference topbar, remove fake identity, bind only existing safe actions and render unsupported controls truthfully | `frontend/src/platform/navigation/Topbar.tsx`; `frontend/src/platform/navigation/Topbar.css`; `frontend/src/platform/navigation/Topbar.test.tsx`; `frontend/src/components/topbar/index.ts`; `frontend/src/components/topbar/TimeRangeSelector.tsx`; `frontend/src/components/topbar/TopbarActions.tsx`; `frontend/src/components/topbar/UserMenu.tsx`; `frontend/src/components/topbar/WorkspaceSwitcher.tsx` |
| First row | Rework 6 investigation KPIs | Replace with the exact five reference card shells and asymmetric geometry | `frontend/src/workspaces/soc/pages/DashboardPage.tsx`; `frontend/src/workspaces/soc/pages/DashboardPage.test.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.css`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.test.tsx` |
| Second row | Absent | Add the five reference panels with real projections or explicit unavailable states | `frontend/src/workspaces/soc/pages/DashboardPage.tsx`; `frontend/src/workspaces/soc/pages/DashboardPage.test.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.css`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.test.tsx` |
| Lower left | Absent | Add AI Agents table geometry; no fake agents, counts, activity, performance or pagination totals | `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.css`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.test.tsx` |
| Lower right | Absent | Add selected-agent geometry with a truthful no-agent/not-configured state | `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.css`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.test.tsx` |
| Active investigation cockpit | Dominant current dashboard | Remove it as the dashboard composition; preserve accepted Finding/Incident drill-down behavior through truthful links or subordinate context only where the reference permits | `frontend/src/workspaces/soc/pages/DashboardPage.tsx`; `frontend/src/workspaces/soc/pages/DashboardPage.test.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.tsx`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.css`; `frontend/src/workspaces/soc/dashboard/EnterpriseSOCDashboard.test.tsx` |

## Rework 7 Data Integrity Projection

- `GET /api/findings` is authoritative for Total Findings and vendor-severity
  distribution only. Counts must be computed from the successfully loaded
  canonical collection and labeled accordingly.
- `GET /api/incidents` may supply canonical incident rows/statuses. Do not
  invent an `active` lifecycle definition; if the backend contract does not
  authoritatively define it, the Active Investigations value is `Unavailable`
  while canonical statuses may be shown separately.
- Overall Risk Score, Risk Trend, Exposed Assets, Top Risky Assets and their
  trends/rankings have no approved productive dashboard read model. Preserve
  their exact mockup geometry but show `Unavailable` rather than deriving or
  inventing risk/exposure semantics.
- There is no approved AI-agent inventory, status, performance, activity,
  capability, cost or execution API. AI Agents Status, the table and selected
  agent detail must show one compact `Not configured`/empty state inside the
  reference geometry. Do not reinterpret the AI Model Registry as AI agents.
- AI Insights must remain `Unavailable`; opening the dashboard must not call a
  provider, model, reasoning or explanation-generation path.
- Recent Decisions remains `Unavailable` in Rework 7. Repository inspection
  found no typed frontend Decision read boundary, and the legacy untyped engine
  response is not authorized as dashboard truth by this task.
- The static `Max Mustermann`/`Security Admin` identity is not authoritative.
  Use trusted existing session metadata only through an existing boundary, or
  render a neutral unavailable operator state. Never expose credentials.
- Do not copy any number, agent, identifier, percentage, timestamp, confidence,
  cost, trend, severity count, activity or security conclusion from the image.

## Rework 7 Functional and Security Boundaries

- Preserve Dashboard, Findings, Investigations, Assets, Threat Intelligence,
  Explainability, Exposure, Decision Center and existing cross-workspace routes.
- Preserve TASK-0121 pathname-authoritative Back, Forward, deep-link, refresh
  and sidebar/content synchronization and its Explainability unavailable state.
- `Explain This Dashboard` must use the existing Explainability route and must
  not execute a provider or fabricate an explanation.
- Controls without an approved behavior must be visibly disabled or carry a
  truthful unavailable state; no decorative element may masquerade as a
  working control.
- Do not create a second router, backend aggregation endpoint, agent domain,
  provider call, mock repository, synthetic runtime dataset or generalized
  dashboard framework.
- Preserve responsive behavior, keyboard accessibility, reduced-motion
  behavior and stable React keys.

## Rework 7 Acceptance Criteria

- At `1536 x 1024` and 100% browser zoom, direct image comparison confirms the
  same full-page composition, region ordering, asymmetric grid, panel geometry,
  hierarchy, density, navy palette, purple accent treatment and thin borders as
  the immutable reference.
- The Sidebar and Topbar match their reference geometry and contain the visible
  information architecture specified above without breaking real navigation.
- Both five-panel rows and the lower approximately `632:722` split are present
  in the first desktop viewport.
- Every real displayed value is traceable to an existing authoritative API
  response; every unsupported metric remains visibly `Unavailable` or `Not
  configured` within the same geometry.
- No reference-image mock values or agent records appear in runtime output.
- The previous Rework 6 active-investigation cockpit is not used as the visual
  dashboard architecture.
- No page load or visual interaction invokes AI/provider execution.
- Loading, API error, empty and successful data states preserve the mockup
  geometry without repeated unavailable-card noise.
- Existing Dashboard-to-Finding/Incident/Threat Intelligence/Command Center and
  Explainability navigation remains URL-authoritative with Back, Forward,
  direct deep-link and refresh behavior intact.
- Tests cover reference region structure, five-plus-five panels, lower split,
  truthful data/unavailable projection, absence of mock values and agents,
  topbar/sidebar routing, theme popover interaction and no provider execution.
- Manual Product Owner acceptance uses the immutable image at full desktop
  view, `1536 x 1024`, 100% zoom and includes the explicitly opened theme
  popover comparison.

## Rework 7 Validation Contract

- Focused Dashboard layout/data-state tests.
- Focused Sidebar, Topbar and navigation/history tests.
- Full frontend regression in regular parallel mode.
- Full frontend regression with `--maxWorkers=1`.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard proving only the declared frontend files changed and the reference
  image remained byte-identical.
- Manual screenshot comparison at `1536 x 1024`, 100% zoom, including SHA-256
  verification of the reference before acceptance.

## Rework 7 Non-Goals

- No backend, API, domain, persistence, runtime or `.env` changes.
- No AI-agent implementation, execution, marketplace or configuration UI.
- No risk, exposure, trend, cost, performance or activity computation without
  an authoritative read contract.
- No Provider/AI execution, generated insight or explanation on page load.
- No change to TASK-0120, TASK-0121 or creation of TASK-0123.
- No DONE transition or Product Owner PASS before explicit live acceptance.

## Rework 7 Architect Implementation Review - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 8

- Result: `FAIL / REWORK REQUIRED`.
- Execution ID: `61491996-d28b-42a0-af16-14d7a4e813d8`.
- Execution commit: `519056fdfdecc967d25bece57b55546ac87c6090`.
- Review-envelope commit: `a5532cbbd60237079b77aafbc39f7c522e934392`.
- Scope Guard and persisted technical validators passed, but implementation
  review found functional routing regressions, incomplete mockup visualization
  geometry, insufficient regression coverage and visible character-encoding
  artifacts. Technical validator success did not satisfy the Product Owner gate.
- Product Owner Live Acceptance was not authorized and remains `FAIL / REWORK
  REQUIRED`; no DONE transition or approval is implied.

## Active Execution Authority - REWORK 8 ONLY

Rework 8 is a bounded correction of the failed Rework 7 implementation. The
immutable visual authority, numerical visual contract, exact 22-file authority,
data-integrity rules, prohibitions and non-goals from active Rework 7 remain
binding except where the explicit Rework 8 criteria below strengthen them. No
historical cockpit requirement becomes executable again.

### Rework 8 Required Corrections

1. Restore and preserve the existing, separate real Dashboard drill-downs for
   Finding, Threat Intelligence, Incident and Command Center. Do not replace
   these distinct destinations with only `View All Findings`, invent routes or
   collapse their semantics. Use the existing PredatorAI route contracts and
   preserve URL-authoritative Back, Forward, deep-link and refresh behavior.
2. Add focused regression coverage that exercises every distinct Dashboard
   drill-down and verifies its exact real destination. A test limited to `View
   All Findings` is insufficient.
3. Implement the reference visualization shells and geometry for the Overall
   Risk Score gauge, Risk Trend, Exposed Assets and severity/status donut or
   status visualizations. Unsupported authoritative values remain truthfully
   `Unavailable` or `Not configured` inside those structures. Never fabricate
   scores, trends, exposure, agents, statuses, percentages or security facts.
4. Remove all visible mojibake and verify UTF-8-safe display text, including
   replacement of artifacts such as `Â·`, `â€”` and `â€¦` without weakening or
   changing security semantics.
5. Preserve all other accepted Dashboard behavior, truthful loading/error/empty
   states, the immutable reference geometry, provider-execution prohibition and
   the eight byte-protected legacy cockpit files.

### Rework 8 Acceptance Criteria

- Finding, Threat Intelligence, Incident and Command Center remain separate,
  visible Dashboard actions and navigate to their established real routes.
- Focused tests exercise all distinct drill-downs and fail if any action is
  removed, merged, mislabeled or routed to another destination.
- The Overall Risk Score panel contains the reference gauge geometry even when
  its value is unavailable; no score is synthesized.
- The Risk Trend panel contains the reference trend/chart geometry even when no
  trend series exists; no trend points or direction are synthesized.
- Exposed Assets and Findings-by-Severity/AI-Agent-status panels contain the
  reference donut/status visualization geometry. Real severity aggregation may
  be projected only from canonical Findings; unsupported exposure and agent
  data remain unavailable/not configured.
- Visual structures retain the approved numerical Rework 7 positions, sizes,
  gaps, typography, colors and tolerances at `1536 x 1024`, 100% zoom.
- No visible UI string in the changed scope contains mojibake or malformed UTF-8
  punctuation.
- No mockup number, fake agent, fake identity, invented security datum, provider
  call, backend change or broader navigation flow is introduced.
- Existing loading, error, empty, unavailable and successful states remain
  truthful and existing Dashboard/Explainability/routing behavior does not
  regress.
- Architect Implementation Review must inspect the rendered structures and
  route wiring directly; green automated validators alone cannot establish PASS.
- Product Owner Live Acceptance remains blocked until Architect Implementation
  Review explicitly confirms all criteria above.

### Rework 8 Validation Requirements

- Focused `EnterpriseSOCDashboard` and `DashboardPage` tests covering all four
  separate drill-down destinations, truthful visualization shells and all
  loading/error/empty/unavailable projections.
- Focused Sidebar, Topbar, SOC routing/history and WorkspaceOutlet regression
  tests within the existing exact file authority.
- Full frontend regression in regular parallel mode and with `--maxWorkers=1`.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard proving changes are limited to the exact 22 allowed paths and all
  prohibited files, including the eight legacy cockpit files and reference PNG,
  remain unchanged.
- SHA-256 verification that
  `.ai/references/TASK-0122-product-owner-mockup.png` remains
  `af672a97050c5e535199d8d006012cf000e5e7b9b66693a02817c4356eea80df`.
- Source and rendered-text inspection for visible mojibake patterns, including
  `Â`, `â€` and replacement-character artifacts.
- Direct implementation-review inspection confirming separate routes, gauge,
  trend, donut and status geometries and absence of invented product data.
- Product Owner screenshot comparison at `1536 x 1024`, 100% zoom remains the
  final gate and is not replaced by automated validation.

## Rework 8 Architect Implementation Review - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 9

- Result: `FAIL / REWORK REQUIRED`.
- Recovery contract: `architect-task-0122-rework-8-recovery-1`.
- Execution ID: `58263571-b5dc-4a66-9415-3c9e4356d8a1`.
- Execution commit: `9aaefc459ad8fe7c72638bd74692f83abfba64b8`.
- Review-envelope commit: `8ce90a163af384514a09fe6861467f912afd5ca4`.
- Scope Guard and persisted technical validators passed. Architect review found
  visible mojibake, a 12px Exposed-Assets height deviation, an overflowing lower
  split, insufficient trend geometry and tests that did not prove the numerical
  or encoding contract. Technical validator success did not satisfy review.
- Product Owner Live Acceptance was not authorized and remains `FAIL / REWORK
  REQUIRED`; no DONE transition or approval is implied.

## Rework 9 Execution Authority - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 10

Rework 9 is limited to correcting the current Architect-review blockers. The
immutable PNG, numerical Rework 7 visual contract, exact 22-file authority,
Rework 8 routing corrections, data-integrity rules, prohibitions and non-goals
remain binding. No backend, provider, fake-data or broader redesign authority is
added.

### Rework 9 Required Corrections

1. Remove every visible mojibake/UTF-8 artifact from Dashboard text, including
   `Â·`, `â€”`, `â€¦` and comparable sequences. Add a regression boundary that
   inspects all visible Dashboard text for known mojibake or replacement markers,
   rather than testing only one literal.
2. Implement panel-specific numerical geometry. Exposed Assets must be `230px`
   high within the approved `+/-4px` tolerance; a shared `218px` row-height rule
   must not flatten its distinct target height. Preserve every other measured
   panel position and size within the active Rework 7 tolerances.
3. Make the lower `632:722` split fit inside the actual padded main content box.
   Explicitly account for left/right widths, gap, outer padding and borders so
   the split neither overflows nor clips at `1536 x 1024` and 100% zoom.
4. Replace the four-horizontal-line placeholder with a truthful chart shell that
   has a recognizable plot area, axes/baseline and a non-data trend-path geometry.
   The unavailable presentation must not encode invented measurements, dates,
   direction or time-series points.
5. Strengthen tests beyond ARIA-role existence. Use robust structure/style
   contract assertions for panel-specific dimensions, lower-split arithmetic,
   absence of overflow, chart axes/path structure and the visible-text encoding
   boundary.
6. Preserve the four separate real drill-downs and their exact routing tests,
   Gauge, Donut and status shells, truthful states, loading/error/empty behavior,
   Explainability and URL-authoritative navigation without regression.

### Rework 9 Numerical Acceptance Criteria

- Exposed Assets resolves to `230px +/-4px`; no common primary-row height may
  force it to `218px`.
- Overall Risk Score is `249x218px`, Total Findings `201x218px`, Active
  Investigations `185x218px` and Risk Trend `303x218px`, each within `+/-4px`.
- All five second-row panels retain their declared Rework 7 widths and `213px`
  height within `+/-4px`.
- The lower visual target remains a `632:722` proportional split. Its implemented
  CSS arithmetic, including borders, padding and any gap, must fit the available
  padded main width without horizontal overflow or clipping. Where fluid sizing
  is required, the ratio must remain equivalent within the approved `+/-4px`
  region tolerance.
- Outer insets and panel gaps remain within `+/-3px`; borders/radii within
  `+/-1px`; typography within `+/-1px`; reference color channels within `+/-8`.
- The trend shell contains explicit plot-area, axis/baseline and curved/path-like
  geometry. It must not consist only of repeated horizontal rules and must not
  assert real observations or trend direction.
- Gauge, exposure/severity Donut and agent-status visualization geometry remains
  present and truthful, with unsupported values still `Unavailable` or `Not
  configured`.
- No visible Dashboard text contains `Â`, `â€`, `�` or another known mojibake
  sequence after rendering.
- Finding, Threat Intelligence, Incident and Command Center actions retain their
  exact real destinations and all four regression tests pass.
- No reference number, fake agent, fake identity, invented product value,
  provider execution or backend change is introduced.

### Rework 9 Validation Requirements

- Focused Dashboard and four-destination routing regressions.
- Geometry-contract tests for every relevant panel height/width, panel-specific
  Exposed Assets height, lower-split fit and overflow prevention.
- Structural trend tests proving plot area, axes/baseline and path geometry and
  rejecting the former four-horizontal-line placeholder.
- Rendered Dashboard text regression rejecting known mojibake and replacement
  markers across all visible text.
- Full frontend regression in regular parallel mode and with `--maxWorkers=1`.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard proving only the exact 22 allowed paths changed and all prohibited
  files, including the eight legacy cockpit files and reference PNG, are intact.
- SHA-256 verification that the immutable reference remains
  `af672a97050c5e535199d8d006012cf000e5e7b9b66693a02817c4356eea80df`.
- Direct Architect inspection of numerical panel geometry, lower-split arithmetic,
  trend shape, encoding output, data truthfulness and all four route bindings.
- Final Product Owner screenshot comparison at `1536 x 1024`, 100% zoom; automated
  checks do not replace this gate.

## Rework 9 Architect Implementation Review - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 10

- Result: `FAIL / REWORK REQUIRED`.
- Execution ID: `077fd0f7-5f7d-482f-b91d-a61284556efe`.
- Execution commit: `d1b7b142eebb52bcb421b5259c7514cbf6999fde`.
- Review-envelope commit: `6cc426e3a56ee5a74a8a0498c13d70a7f94a7c2f`.
- The four real drill-downs, UTF-8 boundary, panel-specific dimensions, lower
  split, Gauge, Donut, status visualization and technical validators passed.
- Architect review found two remaining blockers: the primary-row layout block
  still used `218px` while Exposed Assets used `230px`, permitting a 2px
  primary/secondary overlap after the 10px gap; and the unavailable trend shell
  still rendered a rising/falling SVG series that implied unsupported direction.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED`; no DONE
  transition or approval is implied.

## Rework 10 Execution Authority - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 11

Rework 10 is limited to correcting the two remaining Rework 9 Architect-review
blockers. The immutable PNG, exact 22-file authority, numerical Rework 7 visual
contract, Rework 8 routing corrections, Rework 9 accepted criteria, all
prohibitions, data-integrity rules and non-goals remain binding. No additional
visual redesign, product behavior, backend, provider or data authority is added.

### Rework 10 Required Corrections

1. Correct the primary/secondary vertical layout arithmetic so the primary row
   reserves its actual maximum child height. Exposed Assets remains `230px`
   within `+/-4px`; other primary and secondary panel dimensions remain within
   their existing tolerances. The secondary row must start only after the full
   primary maximum height plus the required row gap. No panel may overlap, clip
   or create horizontal or vertical overflow.
2. Keep a recognizable unavailable trend/chart shell with plot area, frame and
   axes/baseline, but remove every series, point or path shape that asserts a
   rise, fall, fluctuation, measurement or trend direction. The unavailable
   state must be visibly and semantically neutral while retaining the approved
   chart geometry.
3. Add focused contract tests for the complete vertical arithmetic and neutral
   trend semantics. Preserve all previously passing routing, encoding,
   geometry, truthfulness and state regressions.

### Rework 10 No-Overlap Acceptance Criteria

- The computed/declared primary layout block is at least the maximum actual
  primary-panel height; it must not remain fixed at `218px` while any child may
  resolve to `230px`.
- Exposed Assets resolves to `230px +/-4px`; Overall Risk Score, Total Findings,
  Active Investigations and Risk Trend retain `218px +/-4px`; second-row panels
  retain `213px +/-4px`.
- The implemented layout contract satisfies
  `secondaryRowTop >= primaryRowTop + maxPrimaryPanelHeight + requiredGap`.
- The required inter-row gap remains the Rework 7 target within `+/-3px` and is
  measured after the tallest actual primary panel, not after a shorter shared
  track assumption.
- Focused tests evaluate the full arithmetic and fail for the former
  `218px primary track + 230px child + 10px gap` overlap configuration.
- No primary/secondary overlap, clipping, or horizontal/vertical overflow is
  present at `1536 x 1024`, 100% zoom. Existing lower `632:722` split and all
  other numerical tolerances remain satisfied.

### Rework 10 Truthful Trend Acceptance Criteria

- The unavailable Risk Trend panel retains a visible chart plot area,
  chart/frame boundary and X/Y-axis or baseline structure consistent with the
  immutable mockup geometry.
- It renders no data-series path, apparent measurement point, rising segment,
  falling segment, fluctuating contour, fake time series or implied positive or
  negative direction when no authoritative series exists.
- A truthful visible unavailable label/state remains inside the chart shell and
  is not represented as successful trend output.
- The trend regression inspects semantic/structural geometry, not merely
  `role="img"` or the existence of any SVG `path[d]`, and fails if an unavailable
  chart introduces a directional or fluctuating data-series shape.
- No product number, time value, observation, trend direction or security fact
  is invented.

### Rework 10 Preservation Criteria

- Finding, Threat Intelligence, Incident and Command Center retain their
  separate real routes and focused routing regressions.
- UTF-8/mojibake regression remains green; Gauge, Donut, status visualization,
  lower split and all other Rework 9 numerical contracts remain intact.
- Truthful loading, error, empty, unavailable, Explainability and
  URL-authoritative routing behavior does not regress.
- No fake data, invented product value, backend change, provider execution,
  reference change or modification of a prohibited file is introduced.

### Rework 10 Validation Requirements

- Focused `EnterpriseSOCDashboard` and `DashboardPage` tests.
- A vertical geometry/no-overlap contract test proving the complete maximum-row
  height, required-gap and secondary-row-start inequality and rejecting the
  former overlap configuration.
- A truthful-trend semantic/structure test proving visible plot/frame/axis
  geometry while rejecting directional, fluctuating or point-series geometry
  in the unavailable state.
- All four real Dashboard drill-down routing regressions.
- Rendered UTF-8/mojibake regression.
- Full frontend regression in regular parallel mode and with `--maxWorkers=1`.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard proving changes are limited to the exact 22 allowed paths and all
  prohibited files, including the eight legacy cockpit files and reference PNG,
  remain unchanged.
- SHA-256 verification that the immutable reference remains
  `af672a97050c5e535199d8d006012cf000e5e7b9b66693a02817c4356eea80df`.
- Direct Architect inspection confirming no overlap, retained `230px +/-4px`
  Exposed Assets height, actual required row gap, complete vertical arithmetic,
  visible neutral chart structure and absence of invented trend semantics.
- Product Owner screenshot comparison at `1536 x 1024`, 100% zoom remains the
  final gate and is not replaced by automated validation.

### Rework 10 Completion Gate

- Architect Implementation Review remains `PENDING (REWORK 10)` until every
  validation and both direct blocker inspections pass.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED` until a new
  explicit live PASS.
- No DONE transition and no TASK-0123 creation are authorized.

## Rework 10 Architect Implementation Review and Live Acceptance - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 11

- Architect Implementation Review: `PASS / APPROVED`.
- Execution ID: `3510e33c-5fc8-400b-a423-683e7546c5e5`.
- Execution commit: `4702df308175a83e5193624562e8c7a97a63a10e`.
- Review-envelope commit: `383bdac6eafed309ee5dd766be96483546dfe51c`.
- Product Owner Live Acceptance: `FAIL / REWORK REQUIRED` due to three
  cross-workspace regressions: Executive content inset, Main scroll ownership
  and discoverable Executive-to-SOC workspace navigation.
- No DONE transition or final Product Owner approval is implied.

## Rework 11 Execution Authority - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 12

Rework 11 is limited to the three confirmed cross-workspace regressions below.
All accepted Rework 10 SOC visual, geometry, truthful-data, routing, UTF-8 and
provider-execution constraints remain binding preservation requirements. No
backend, API, domain, runtime, `.env`, provider or broader redesign authority is
added. `WorkspaceOutlet.tsx` remains outside the allowed scope unless a new
root cause is separately proven and authorized; no such authority exists now.

### Rework 11 A - Executive Content Offset and Workspace Inset

- Preserve the complete SOC Dashboard geometry from Rework 10; do not restore
  global `p: 3` if that shifts the SOC mockup.
- Give Executive an explicit workspace-owned content inset using normal
  Grid/Flex/padding sizing. Do not use negative margins, transforms or offset
  hacks.
- Main content begins entirely to the right of the `164px` Sidebar track.
- Executive headings and panels are neither covered by the Sidebar nor clipped
  at the left edge.
- Tests prove the Sidebar track, Main start, Executive inset and absence of a
  negative-offset workaround.

### Rework 11 B - Main Scroll Ownership and Overflow

- Bind the platform root to the viewport and give the right Grid column explicit
  `min-width: 0`.
- Apply `min-height: 0` through the required right-column/Main chain so Main,
  rather than the document or an unconstrained ancestor, owns scrolling.
- Define Main `overflow-x` and `overflow-y` explicitly. Never force a scrollbar
  merely to conceal incorrect width arithmetic.
- At `1536 x 1024`, 100% zoom, Executive fits horizontally inside the available
  post-Sidebar content box with no unnecessary scrollbar, clipping, inaccessible
  content or Sidebar overlap.
- At a genuinely smaller viewport, real overflow remains reachable at the
  intended Main scroll container.
- SOC-owned scrolling and the immutable Rework 10 visual contract remain intact.
- Tests verify scroll ownership, `min-width: 0`, the required `min-height: 0`
  chain, 1536px Executive fit, smaller-viewport reachability and SOC preservation.

### Rework 11 C - Cross-Workspace Navigation

- Keep the canonical flow `Topbar -> WorkspaceSwitcher -> setWorkspace ->
  navigate -> WorkspaceOutlet`; do not introduce a parallel router or fake
  navigation.
- The switcher visibly identifies the current workspace. `SOC Analyst` and
  `Executive` must each be unambiguous, and the control must visibly communicate
  that it changes workspaces.
- The complete UI flow `SOC Analyst Dashboard -> Executive -> Executive
  Overview -> SOC Analyst -> SOC Dashboard` works without Browser Back, manual
  URL entry or reload.
- Routes remain `/executive` for Executive Overview and `/` for the SOC Analyst
  Dashboard. The correct workspace-specific Sidebar and Dashboard render after
  each change, and URL-authoritative Back/Forward behavior remains intact.
- Remove, adapt or de-emphasize the disabled Sidebar `Enterprise Workspace /
  Unavailable` affordance so it no longer contradicts the functional global
  switcher. Do not create a second workspace authority.
- A full structure-level regression test exercises the visible switcher, both
  routes, both workspace labels, both Sidebars/Dashboards and the return path.

### Rework 11 Exact Additional File Authority

The existing exact Rework 10 scope remains authorized only as needed for
preservation and focused regression. Rework 11 adds exactly:

- `frontend/src/platform/layout/PlatformLayout.test.tsx` - platform viewport,
  Sidebar/Main geometry and scroll-owner contract.
- `frontend/src/workspaces/executive/ExecutiveLayout.tsx` - Executive-owned
  content inset and bounded width.
- `frontend/src/workspaces/executive/ExecutiveLayout.test.tsx` - Executive inset,
  fit and overflow contract.

No wildcard or implicit additional file authority is granted.

### Rework 11 Acceptance Criteria

- A: Executive owns a positive explicit content inset; its content begins after
  the Sidebar/Main boundary with no overlap, clipping or negative offset hack.
- B: Platform root is viewport-bound; the right column has `min-width: 0`; the
  required parent/Main chain has `min-height: 0`; Main explicitly owns both axes
  of overflow.
- B: Executive has no unnecessary horizontal overflow at `1536 x 1024`, while
  real smaller-viewport overflow remains reachable on Main. No content is
  inaccessible and no scrollbar is artificially forced.
- C: the global switcher prominently exposes the current `SOC Analyst` or
  `Executive` identity and performs the canonical `/ -> /executive -> /` flow.
- C: matching workspace Sidebar and Dashboard content render after each switch;
  no navigation dead-end, manual URL, reload or Browser Back is required.
- Existing Back/Forward and pathname authority remain correct.
- Every Rework 10 result remains passing, including `230px` Exposed Assets,
  no-overlap arithmetic, truthful neutral Trend, Gauge, Donut, status shell,
  lower `632:722` split, four SOC drill-downs, UTF-8, truthful states, no fake
  data, no backend changes and no provider calls.

### Rework 11 Validation Requirements

- Focused `PlatformLayout` and `ExecutiveLayout` geometry/overflow tests.
- Focused Sidebar, Topbar and WorkspaceSwitcher interaction tests.
- Full cross-workspace structure regression:
  `SOC Dashboard -> Executive Overview -> SOC Dashboard`, including correct
  route, current-workspace label, Sidebar and rendered workspace content.
- `WorkspaceOutlet` route/history regressions without changing its production
  implementation.
- Focused Rework 10 SOC Dashboard geometry, truthful Trend, drill-down and UTF-8
  regressions.
- Full frontend regression in regular parallel mode and with `--maxWorkers=1`.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard proving only the exact 25 allowed paths changed and every
  prohibited path remains intact.
- SHA-256 verification that the immutable reference remains
  `af672a97050c5e535199d8d006012cf000e5e7b9b66693a02817c4356eea80df`.
- Direct Architect inspection of Executive offset/inset, scroll ownership,
  cross-workspace navigation and preservation of all Rework 10 results.
- Product Owner live acceptance at `1536 x 1024`, 100% zoom, including SOC to
  Executive to SOC and scroll/overflow behavior. Automated checks do not replace
  this gate.

### Rework 11 Completion Gate

- Architect Implementation Review remains `PENDING (REWORK 11)`.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED` until explicit
  new live PASS.
- No DONE transition and no TASK-0123 creation are authorized.

## Rework 11 Architect Implementation Review - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 12

- Result: `FAIL / REWORK REQUIRED`.
- Execution ID: `3ade6faa-a870-4b93-b901-78c19a920324`.
- Execution commit: `6da17fe3dfdcd47a92427d5eb35d62f046984918`.
- Review-envelope commit: `6fad96a84f458740b84a0096956203a415730df6`.
- Scope compliance and persisted frontend, TypeScript, production-build and
  `git diff --check` validators passed.
- Executive inset and Sidebar/Main separation passed. Architect review retained
  three blockers: the workspace switcher remained visually undiscoverable, the
  cross-workspace test replaced both real workspace compositions with generic
  doubles, and the layout tests asserted styles without proving the 1536px fit
  or reachable smaller-viewport overflow.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED`; no DONE
  transition or final Product Owner approval is implied.

## Rework 12 Execution Authority - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 13

Rework 12 is limited to the three remaining Rework 11 Architect-review blockers.
The accepted Executive inset implementation is frozen except where a narrowly
necessary test seam is required to prove its existing arithmetic; no unnecessary
product rewrite is authorized. All Rework 10 preservation requirements and all
existing prohibitions remain binding. No backend, API, domain, runtime, `.env`,
provider, routing-architecture or broader visual authority is added.

### Rework 12 Blocker 1 - Workspace Switcher Discoverability

- Keep the canonical `WorkspaceSwitcher -> setWorkspace -> navigate` mechanism.
  Do not introduce a second workspace authority or routing path.
- Render the current `SOC Analyst` or `Executive` identity prominently enough to
  be found by a sighted user and make the control visibly select-like/clickable.
- Use the existing PredatorAI Topbar visual language with a bounded control
  surface, legible typography, visible affordance and focus/interaction styling.
  The current `9px`, muted, transparent, borderless and unpadded presentation is
  not acceptable.
- Do not replace the current identity with a generic `Enterprise Workspace`
  label and do not perform a broader Topbar redesign.
- Preserve the Rework 10 SOC visual contract and existing accessible menu name,
  keyboard semantics, enabled-workspace filtering and canonical destinations.

### Rework 12 Blocker 2 - Real Cross-Workspace Integration

- Add a deterministic integration regression for `SOC Analyst Dashboard ->
  WorkspaceSwitcher -> Executive -> Executive Overview -> WorkspaceSwitcher ->
  SOC Analyst -> SOC Dashboard` using `/ -> /executive -> /`.
- For this integration test, do not replace `SOCWorkspace` or
  `ExecutiveWorkspace` with generic workspace-level text doubles. Render their
  real production composition and assert identifiable real SOC Dashboard and
  Executive Overview content.
- Verify the matching real Sidebar, current workspace identity and pathname after
  each switch. Browser Back, reload and manual URL entry are not part of the
  successful flow.
- Lower-level network/API dependencies may be deterministically isolated only at
  their existing client/repository boundaries. Do not mock away the workspace,
  page, layout, router or navigation composition under test and do not introduce
  synthetic security data.
- Preserve the existing URL-authoritative Back/Forward regressions and leave
  `frontend/src/platform/workspace/WorkspaceOutlet.tsx` byte-identical.

### Rework 12 Blocker 3 - Fit and Overflow Proof

- Retain the accepted platform contract: `1536px` viewport width, `164px`
  Sidebar track, `1372px` post-Sidebar column, viewport-bound root,
  `min-width: 0`, `min-height: 0` and Main-owned overflow on both axes.
- Prove the 1536px Executive fit with deterministic layout arithmetic using the
  actual contract values: the Executive border-box width must not exceed the
  `1536 - 164 = 1372px` Main client width, and its `24px` inset must remain
  internal through `box-sizing: border-box`. The proof must reject arithmetic
  that places content outside Main or assigns overflow ownership to the document.
- Add a smaller deterministic viewport fixture whose authorized test content has
  a declared minimum width greater than Main's client width. Model or measure
  `scrollWidth > clientWidth`, assert Main is the owning scroll container and
  prove the complete horizontal extent is reachable through Main.
- Because jsdom does not perform browser layout, style/source assertions alone
  must not be described as pixel measurement. Use explicit, reusable test-only
  viewport/sidebar/inset/content-width arithmetic plus controlled DOM dimension
  properties, or an already configured repository browser mechanism. The tests
  must fail for incorrect Sidebar subtraction, external inset addition, missing
  overflow, or document-owned scrolling.
- Do not force a scrollbar or introduce production width hacks merely to satisfy
  the test. The accepted Executive `24px` inset and PlatformLayout implementation
  remain unchanged unless a demonstrated defect requires the smallest correction.

### Rework 12 Acceptance Criteria

- The WorkspaceSwitcher visibly presents the exact current workspace and is
  clearly recognizable as an interactive workspace-selection control without a
  broader Topbar redesign.
- `SOC Analyst` and `Executive` remain legible, accessible and correctly routed
  through the existing `setWorkspace()` and `navigate()` boundary.
- A real-composition integration test renders the actual SOC Dashboard and actual
  Executive Overview and proves `/ -> /executive -> /`, matching Sidebars and
  current-workspace labels without Back, reload or manual URL entry.
- The real-composition test does not mock `SOCWorkspace` or `ExecutiveWorkspace`;
  only lower-level external data boundaries may be controlled.
- Deterministic arithmetic proves that at `1536 x 1024`, the `164px` Sidebar and
  `1372px` Main area contain the Executive border box, including its internal
  `24px` inset, without horizontal overflow, clipping or inaccessible content.
- A defined smaller viewport proves genuine `scrollWidth > clientWidth` for
  deliberately wider test content, Main-owned overflow and reachability of the
  complete horizontal extent without document/body scroll ownership.
- Tests state their jsdom limitation truthfully and do not claim CSS property
  assertions alone constitute browser pixel-layout evidence.
- Rework 11 A remains passing: `24px` Executive inset, `164px` Sidebar track,
  no negative margins/transforms and no Sidebar overlap regression.
- Every Rework 10 result remains passing: SOC visual contract, Exposed Assets
  `230px`, no-overlap arithmetic, truthful neutral Trend, Gauge, Donut, status
  visualization, lower `632:722` split, four SOC drill-downs, UTF-8, truthful
  states, no fake data, no backend changes and no provider calls.

### Rework 12 Validation Requirements

- Focused `WorkspaceSwitcher`, Topbar and Sidebar interaction/presentation tests.
- Real-composition cross-workspace integration test covering actual SOC Dashboard
  and Executive Overview through `/ -> /executive -> /` with matching Sidebars
  and current-workspace labels.
- Focused `PlatformLayout` and `ExecutiveLayout` contract tests with explicit
  1536px fit arithmetic and controlled smaller-viewport `scrollWidth >
  clientWidth`/Main reachability evidence.
- Existing `WorkspaceOutlet` route/history regressions without changing its
  production implementation.
- Focused Rework 10 SOC Dashboard geometry, truthful Trend, drill-down and UTF-8
  regressions.
- Full frontend regression in regular parallel mode and with `--maxWorkers=1`.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard proving changes are limited to the exact 25 allowed paths and every
  prohibited path remains unchanged.
- SHA-256 verification that the immutable reference remains
  `af672a97050c5e535199d8d006012cf000e5e7b9b66693a02817c4356eea80df`.
- Direct Architect inspection of switcher discoverability, real workspace
  composition, fit/overflow arithmetic, Rework 11 A and Rework 10 preservation.
- Product Owner live acceptance at `1536 x 1024`, 100% zoom, exercising SOC
  Dashboard to Executive Overview and back plus scroll/overflow behavior.
  Automated checks do not replace this gate.

### Rework 12 Completion Gate

- Architect Task-Specification Review: `PASS / APPROVED (REWORK 12)`.
- Architect Implementation Review remains `PENDING (REWORK 12)`.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED` until explicit
  new live PASS.
- No DONE transition and no TASK-0123 creation are authorized.

## Rework 12 Architect Implementation Review - HISTORICAL / NON-EXECUTABLE / SUPERSEDED BY REWORK 13

- Result: `FAIL / REWORK REQUIRED`.
- Recovery contract: `architect-task-0122-rework-12-recovery-1`.
- Execution ID: `f196f4f9-6a4c-4ab8-9f20-690df90f84b8`.
- Execution commit: `6e5dffd00e0ee6aeb9f224fc0307f9a8d16d3581`.
- Review-envelope commit: `5c28c1cb0ffaccb254946643d252f09fcdfb02ad`.
- WorkspaceSwitcher discoverability, the real SOC-to-Executive-to-SOC integration
  flow and all preservation gates passed.
- The fit/overflow proof retained three test-only blockers: the 1536px test
  assigned the Executive border-box width from Main instead of measuring two
  independent controlled dimensions; the smaller-viewport test did not inspect
  `document.body`; and the tests did not explicitly state that their controlled
  jsdom dimensions are deterministic contract evidence rather than native pixel
  measurement.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED`; no DONE
  transition or final Product Owner approval is implied.

## Active Execution Authority - REWORK 13 ONLY

Rework 13 is limited to correcting the three remaining test/evidence defects in
the Rework 12 fit/overflow proof. The production implementation is frozen. No
production file may change unless a new concrete product defect is first proven
and separately authorized; this contract grants no such authority. Rework 12
Blockers 1 and 2 and all preservation results remain accepted and frozen.

### Rework 13 Blocker 3.1 - Independent 1536px Fit Evidence

- Use independent controlled dimensions for the `1536px` viewport model:
  `viewportWidth=1536`, `sidebarWidth=164`, expected Main width `1372`, Executive
  horizontal inset `24px + 24px` and expected content width `1324`.
- Model `main.clientWidth`, `main.scrollWidth`, Executive border-box/client width
  and the Executive horizontal start/offset condition independently. Do not set
  Executive width by assigning it from `mainClientWidth`.
- Assert Main client width equals `1372px`; Executive's right edge is less than
  or equal to Main's right edge; Main `scrollWidth === clientWidth`; the `24px`
  inset remains internal through `box-sizing: border-box`; and no external
  positive margin or offset contributes to the Executive footprint.
- Structure the assertions or a small test-local contract evaluator so deliberately
  invalid fixtures for an oversized Executive width and for a positive external
  offset both fail the fit predicate. The production component is not changed.
- The test must distinguish the independent measured/modelled values from the
  expected contract constants and must not make the result true by construction.

### Rework 13 Blocker 3.2 - Body Scroll Ownership

- Retain the smaller deterministic viewport model:
  `viewportWidth=1024`, `sidebarWidth=164`, Main `clientWidth=860`, controlled
  content/Main `scrollWidth=1100` and maximum Main `scrollLeft=240`.
- Assert Main `scrollWidth > clientWidth`, set Main to its maximum horizontal
  offset and prove `scrollLeft + clientWidth === scrollWidth`.
- Independently control and assert both `document.documentElement` and
  `document.body` with `scrollWidth === clientWidth`, so neither owns additional
  horizontal overflow.
- Include a deliberately invalid body-overflow fixture or equivalent test-local
  predicate assertion that would fail if Body became the horizontal scroll owner.

### Rework 13 Blocker 3.3 - Explicit jsdom Boundary

- Place an explicit test name and/or adjacent comment beside the controlled
  dimension model stating that jsdom does not perform native pixel layout.
- State that the test is deterministic layout-contract verification using
  controlled DOM dimensions and is not a native browser pixel measurement.
- State that actual rendering remains subject to Product Owner Live Acceptance at
  `1536 x 1024`, 100% browser zoom.

### Rework 13 Exact Scope

- `frontend/src/platform/layout/PlatformLayout.test.tsx` - smaller-viewport Main,
  documentElement and Body scroll-ownership model plus explicit jsdom boundary.
- `frontend/src/workspaces/executive/ExecutiveLayout.test.tsx` - independent
  1536px fit model and invalid width/offset regression fixtures.

No wildcard, implicit test path or production-file authority is granted.

### Rework 13 Acceptance Criteria

- The 1536px test uses independently controlled Main and Executive dimensions;
  it never assigns Executive border-box width from Main client width.
- Main resolves to `1372px`; its fit-state `scrollWidth` equals `clientWidth`;
  Executive content resolves to `1324px` inside the `24px + 24px` border-box
  inset; and the independently modelled Executive footprint stays within Main.
- Deliberately oversized Executive width and positive external-offset fixtures
  fail the fit predicate, proving the test detects both regression classes.
- The smaller viewport proves `1100 > 860`, maximum Main scroll offset `240` and
  complete horizontal reachability through Main.
- Both `document.documentElement` and `document.body` are independently modelled
  without horizontal overflow; a Body-overflow regression fails the predicate.
- Test names/comments explicitly identify controlled jsdom contract modelling as
  non-native pixel evidence and retain the live browser gate.
- WorkspaceSwitcher discoverability and the real `/ -> /executive -> /`
  integration test remain unchanged and passing.
- `WorkspaceOutlet.tsx`, every production layout/navigation file and all Rework
  10 product behavior remain byte-identical to the Rework 12 execution baseline.
- No fake data, backend change, provider execution, DONE transition or TASK-0123
  creation is introduced.

### Rework 13 Validation Requirements

- Focused `PlatformLayout.test.tsx` and `ExecutiveLayout.test.tsx` regressions.
- Existing WorkspaceSwitcher and real cross-workspace integration regressions.
- Focused Rework 10 SOC Dashboard geometry, truthful Trend, routing and UTF-8
  preservation regressions.
- Full frontend regression.
- `npx tsc --noEmit`.
- `npm run build`.
- `git diff --check`.
- Scope Guard proving exactly the two test files are authorized and every
  production/prohibited file remains unchanged.
- SHA-256 verification that the immutable reference remains
  `af672a97050c5e535199d8d006012cf000e5e7b9b66693a02817c4356eea80df`.
- Direct Architect inspection of independent dimensions, invalid fit fixtures,
  Main/documentElement/Body ownership and explicit jsdom-boundary wording.
- Product Owner live acceptance remains required at `1536 x 1024`, 100% zoom.

### Rework 13 Completion Gate

- Architect Task-Specification Review: `PASS / APPROVED (REWORK 13)`.
- Architect Implementation Review remains `PENDING (REWORK 13)`.
- Product Owner Live Acceptance remains `FAIL / REWORK REQUIRED` until explicit
  new live PASS.
- No DONE transition and no TASK-0123 creation are authorized.

## Historical Audit Continuation - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

Every remaining section is retained solely as historical evidence. It is not
part of the active Acceptance Criteria, Execution Authority or Validation
Requirements and cannot override the verified PNG or active Rework 7 sections.

## Historical Product Owner Live Acceptance - Rework 5 - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Result: `FAIL / REWORK REQUIRED`.
- Functional Finding, Threat Intelligence, Incident, Refresh and browser-history
  navigation passed live acceptance.
- Visual parity failed at 1920x1080 and 100% browser zoom: the page still lacks
  five compact KPI instruments, a spatial 2D Investigation Graph, the required
  cockpit density and the intended first-viewport hierarchy.
- No DONE transition or final Product Owner approval is implied.

## Historical Rework 6 Contract - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Contract ID: `architect-task-0122-rework-6`.
- Restrict implementation to the existing Dashboard page and reusable SOC
  cockpit presentation components named in `allowed_scope`.
- Render exactly five compact operational KPI instruments using only canonical
  Finding identity, vendor severity, observed asset identity, persisted
  Finding-to-Incident relationship and loaded Incident/Command Center state.
- Missing KPI values remain explicitly unavailable; do not invent counts,
  priority, confidence, runtime, business impact, MITRE or compromise claims.
- Use separated 2D graph-node positions with an independent SVG edge/label
  layer. Render edges only for relationships established by canonical responses.
- Do not infer CVE or Threat Intelligence identity from Finding title, severity
  or asset. A non-resolved TI node must remain truthfully unavailable.
- Present compact Investigation State plus Evidence, PredatorAI Analysis and
  Recommended Investigation prominently in the first viewport. Without an
  authoritative analysis projection, Analysis must remain explicitly unavailable.
- Preserve distinct Finding, Threat Intelligence, Incident and Command Center
  actions and their existing routes; preserve truthful loading, error, empty and
  unavailable states and all accepted functional navigation behavior.
- Opening the dashboard must not execute a provider/model or instantiate mock,
  synthetic or hard-coded security data.
- Add focused coverage for the five KPIs, truthful values/unavailable states,
  spatial graph geometry and relationship labels, analysis unavailability,
  first-viewport composition and the four distinct drill-down actions.
- Run focused Dashboard/cockpit tests, full frontend regression, TypeScript,
  production build, `git diff --check`, Scope Guard and manual Product Owner
  acceptance at 1920x1080 / 100% zoom.
- `frontend/src/workspaces/soc/SOCWorkspace.test.tsx` and
  `frontend/src/workspaces/threat-hunter/pages/HuntHypothesesPage.test.tsx` are
  authorized only for a locally bounded timeout stabilization of the two
  diagnosed tests. Do not change, remove or weaken assertions; do not change
  test logic, mock data or expected values; do not skip, quarantine, use
  `.only`, serialize tests or change global Vitest configuration. Use the
  smallest reliably sufficient per-test timeout.
- TASK-0122 remains subject to Architect review and the Product Owner gate; do
  not create TASK-0123 or transition TASK-0122 to DONE.

## Historical Architect Review - Rework 6 - NON-EXECUTABLE / SUPERSEDED BY REWORK 7

- Result: `PASS / APPROVED`.
- Contract: `architect-task-0122-rework-6-retry-2`.
- Execution ID: `511e9a21-8595-4356-8a7d-013ab568c247`.
- Execution commit: `69e6f83342238616bc218c7bd944d1dc138c0c2e`.
- Review Envelope commit: `fefd51bc0d48c62f83a6c98777a10348a82a3d02`.
- Scope Guard: `COMPLIANT`.
- Persisted frontend tests, TypeScript, production build and `git diff
  --check`: `PASS`.
- Product Owner Live Acceptance remains `PENDING`; no DONE transition or final
  Product Owner approval is implied.
