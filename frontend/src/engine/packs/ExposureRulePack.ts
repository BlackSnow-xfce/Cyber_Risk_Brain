import type { RulePack } from "../RulePack";

export const exposureRulePack: RulePack = {
    id: "exposure",
    name: "Exposure Rules",
    description: "Rules for exposure-specific context.",
    version: "1.0.0",
    enabled: true,
    rules: [],
};
